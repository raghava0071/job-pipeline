[1.8.9] 2026-07-12 — DIAGNOSTIC ONLY, still no fix applied. Raghav's specific hypothesis after 1.8.8: `_captcha_actually_visible()` might be reading STALE leftover state from a previous CAPTCHA — a token/aria-checked/frame reference left over from an earlier challenge — causing "solved" to fire implausibly fast on a NEW one. Asked for four things before any change: (1) is the token explicitly cleared by our code or just read as-is, confirmed by logging it at the moment of detection; (2) is any frame/element reference cached across retries or re-queried fresh each time; (3) a log line at detection time with token value, aria-checked value, and a way to compare element identity across attempts; (4) don't fix anything, just add the logging and report.

Answered (1) and (2) directly from the code, no new logging needed for these two:
- (2) NO caching, confirmed by re-reading every relevant call site: `_captcha_actually_visible()` does `for f in list(page.frames)` fresh on every single invocation (both the initial gate-check and every iteration of the 600-tick wait loop) — Playwright's `page.frames` is a live property queried against the actual browser on each access, not a snapshot. `_pin_captcha_box()` does the same. `_check_and_handle_captcha()` itself is called fresh — new function invocation, new nested closures, zero shared local state — "at the start of every step loop iteration AND after every Submit click" per its own docstring, so "attempt 1" and "attempt 2" never share a Python-side handle. The only state that persists across calls is module-level (`_pinned_elements`, `_consecutive_captcha_failures`), and `_pinned_elements` is explicitly `.clear()`ed on every solve path.
- (1) Our code never explicitly clears the token — it only reads `el.value` as-is, trusting Google's own JS to manage it. BUT: the control flow makes a stale-and-nonempty token AT THE DETECTION INSTANT logically impossible, not just unlikely. `_check_and_handle_captcha()` calls `_captcha_actually_visible()` once at the very top and immediately `return`s if it comes back False (token found = "solved" = captcha_visible False) — before the "🚨 CAPTCHA DETECTED" line is ever printed. So by construction, every time that banner appears in a log, the token was already confirmed empty (or ≤10 chars) at that exact moment, or the function would have exited silently instead. This doesn't rule out staleness appearing seconds LATER, inside the wait loop — that's what (3)'s new logging is for.

Implemented (3): `_log_captcha_identity_snapshot(label)`, called once right when "CAPTCHA DETECTED" prints and again right when "✅ CAPTCHA solved!" fires. Captures, for the token textarea and the anchor checkbox: the raw value (length + 12-char preview, not just the boolean derived from it) AND a stable per-element identity marker — a random id stamped into `el.dataset.pwProbeId` the first time we see that exact node, read back unchanged on every later call. That marker is what actually answers the staleness question: if a later "solved" snapshot reports the SAME element_id as an earlier "detected" one, it's the literal same DOM node (expected, and any staleness there is Google's JS not clearing it promptly — not ours). If a later detection reports a DIFFERENT element_id, the two challenges used genuinely different nodes, ruling out a cached Python-side handle as the explanation (there is no handle to cache — see (2)). Each snapshot is tagged with a new module-level `_captcha_detection_seq` counter (`[DETECTED #1]`, `[SOLVED #1]`, `[DETECTED #2]`, ...) so separate detection events can be matched up and compared directly in the log instead of inferred from timestamps.

Syntax-checked (`py_compile`) — clean. Verified with a throwaway mock harness (kept on disk per Raghav's request — `_test_captcha_identity.py`) using a stateful fake token element that mimics real dataset-stamping (id set once, unchanged on every later read): confirmed the DETECTED and SOLVED snapshots for the same simulated CAPTCHA report identical element_id (correctly identifying "same node"), confirmed token length reads 0 at DETECTED and non-zero at SOLVED, and confirmed sequence numbering. No fix written this round — next real CAPTCHA (ideally a multi-round one, since that's the scenario in question) will show directly whether a "solved" element_id matches a prior detection's, which is the concrete evidence needed before writing the actual fix.

[1.8.8] 2026-07-12 — DIAGNOSTIC ONLY, no fix applied yet. Raghav asked to root-cause the recurring `_pin_captcha_box()` positioning bug (a "select all squares with buses" challenge where the Verify button was unreachable) properly this time — find the actual log evidence before patching blind, since this exact function has already gone through three "confirmed still broken" rounds (2026-07-08 through 07-10) each guessing at a slightly different cause.

Step 1 — searched for a log of the failing run: no log file captured the specific "buses" screenshot event (it was an untracked manual terminal session). Searched every log that DOES exist instead: across `scheduler_evening/afternoon/morning_out.log` (which turned out to span June 6 through today, not just today), counted 103 Indeed CAPTCHA detections total, but only 6 explicit "🔲 CAPTCHA window pinned" and 0 "⚠ Could not pin" prints — a huge gap that first looked like the pin function was failing silently ~93% of the time. Ruled that out by isolating the actual MOST RECENT real detection in the log (today's 6pm run, predating this session's earlier fixes) and reading it in full: the pin DID succeed ("🔲 CAPTCHA window pinned"), but "✅ CAPTCHA solved! Auto-clicking Submit..." fired within roughly 1-2 seconds of detection — implausibly fast for a human to have actually solved an image-selection challenge. The auto-click fired, got no confirmation, retried, hit ANOTHER captcha immediately, pinned and "solved" again just as fast, then subsequent submit attempts (4 through 8) all show "⚠ No nav buttons found in any frame" until it gave up with "❌ Failed: form walk ended."

This doesn't cleanly match either branch anticipated going in (detection missing a dynamic multi-round challenge, or styling getting silently reset) — it looks more like a possible race in the "solved" signal itself, though this specific log predates this session's earlier CAPTCHA-overlay-leak fix (the leftover-pinned-overlay-intercepts-the-click bug), so it's not certain whether this exact pattern still reproduces on current code. Given the ambiguity, no fix was written this round — per the explicit instruction to reproduce with better instrumentation before patching again instead of guessing a fourth time.

Step 2 — added the requested diagnostics, all in `indeed_apply_now.py`:
- `_captcha_actually_visible()` now logs which of the three signals (g-recaptcha-response token / anchor aria-checked / bframe visibility+text) is driving each `still_captcha` transition, via a new `_log_signal_transition()` helper — fires only when the (visible, reason) pair actually changes, not once per poll tick (that would be up to 600 near-identical lines for a single stuck job). This directly answers whether "solved" is firing off a real signal or something spurious, and which one.
- New `_log_pin_diagnostics()`, called right after every `_pin_captcha_box()` call (both the initial pin and every ~3s re-pin in the wait loop): independently re-reads each pinned element's real `getBoundingClientRect()` and `getComputedStyle()` — not trusting the boolean `_pin_captcha_box()` returns, which only means the JS assignment didn't throw, not that the style actually rendered or is still in effect. Flags a zero-sized/unrendered element explicitly (`⚠ ZERO-SIZE / NOT ACTUALLY RENDERED`) instead of leaving that to be inferred from a screenshot after the fact.

Syntax-checked (`py_compile`) — clean. Verified with a throwaway mock harness (kept on disk this time at Raghav's request, not deleted — `_test_pin_diagnostics.py`) simulating a bframe that reports itself pinned but renders zero-sized: confirmed the new pin diagnostics correctly flag it as not actually rendered, confirmed signal-transition logging fires only 2 times across a multi-tick wait loop (not per-tick spam), and confirmed the existing solve flow still completes normally — this change adds visibility only, no behavior change. Next real CAPTCHA encounter should produce a log that states definitively, in one read, whether the pin rendered on-screen and which signal drove each solved/unsolved transition — that's what determines which of the two originally-anticipated branches (or the race-condition pattern found in step 1) actually applies, and only then should the real fix get written.

[1.8.7] 2026-07-12 — Raghav caught a serious one: he got a "✅ Application Submitted" email (with resume attached, 89% fit) for Air Treatment Corporation's "Inside Sales Engineer" job, but sent a screenshot of that exact job's actual page at the time — still sitting at 50% progress, 3 required HVAC-industry essay questions ("Have you ever worked in the HVAC industry?", etc.) left completely blank, each showing Indeed's own red "Answer this question to continue" validation error. The form never advanced. The email said it did.

Pulled the run's own JSON log (`data/runs/run_20260712_195358_indeed.json`) to check timing: Air Treatment Corporation's job started 288.3s into the run, and the whole run ended at 292.4s — meaning the pipeline went from starting this job to declaring it "Applied"/"submitted" and firing the confirmation email in about 4 seconds. Not enough time to genuinely fill and submit a multi-question form. Those 3 essay fields are `_is_never_cache` fields (always sent to Claude fresh, never cached) — almost certainly the AI fallback for them wasn't available in whatever environment that run used (same class of issue as 1.8.6), so they came back unanswered, the Continue click failed validation, and the form was stuck exactly as screenshotted.

Root cause: `_is_confirmed()` scans every frame's `innerText` for `CONFIRM_PHRASES` ("application submitted," "you've applied," etc.) and returns True on any match, with one narrow guard — a denylist of known review-page URL/text markers (`review-m`, `review-module`, `you won't be able to edit your application`, etc.), added back in 1.x after an earlier false positive on Indeed's pre-submit review screen. That guard has no coverage for a stuck QUESTIONS step — a completely different page — so a stray confirm-phrase match anywhere in that page's text (footer copy, a promo line, anything) sailed straight through and got reported as a real submission. This is the second time this exact function has produced a false "submitted" from an incomplete page; chasing it page-type by page-type is the same whack-a-mole this function's own comments already flagged after the first round.

Fixed with a property check instead of another page-type guess: when a confirm-phrase match survives the existing review-page guard, `_is_confirmed()` now also checks that same frame for any visible, unresolved validation-error element (reusing the exact selector list — `[aria-invalid="true"]`, `[role="alert"]`, error-class elements, etc. — already proven out elsewhere in this file for "form walk ended" retry handling). If the page still has an unresolved required-field error, the confirm-phrase match is discarded and scanning continues to the next frame instead of declaring success. This generalizes past just review pages or just questions pages — it catches "confirm-phrase text coexisting with a visibly broken form" on ANY page type Indeed might show, present or future.

Syntax-checked (`py_compile`) — clean. Verified with a throwaway mock harness (run and discarded, not part of the pipeline) covering both directions: (1) a page replicating the exact reported bug — confirm-phrase text present alongside an unresolved validation error — now correctly returns "not confirmed" instead of the false positive; (2) a genuine success page with no validation errors still correctly returns "confirmed," confirming this fix doesn't break real success detection. Not yet confirmed against a real stuck Indeed form live.

Also flagged, not yet fixed: Raghav separately reported the reCAPTCHA "Verify" button being hard to reach on a different job's CAPTCHA challenge (a 4x4 "select all squares with buses" grid) — the pinned box should be scrollable (`overflow-y: auto` from 1.8.4's fix) so the button should be reachable by scrolling within the popup, but this needs a live repro to confirm whether that's sufficient or whether `_pin_captcha_box()` needs further work — same function that already went through three "confirmed still broken" rounds in the 2026-07-08 through 07-10 history above.

[1.8.6] 2026-07-12 — Raghav's live run (conda `base`) crashed on the very first form step — an address block (City/State/Zip) — with "No module named 'anthropic'", right after the 1.8.5 diagnosis pointed at a missing package in that environment. Raghav pushed back: he doesn't want to install `anthropic` into every environment he might run this from, and clarified his actual rule — scoring stays exactly as-is, resume building keeps using Claude freely (both untouched here), and Claude should only be invoked for the pipeline's OWN "where every the pipeline is not able to fill any kind of form or boxes."

Checked the code against that rule before changing anything: `smart_fill_step()` (Indeed), `fill_and_submit_form()`'s field-filling closure (LinkedIn), and `_smart_fill_questions()` (Workday) ALREADY implement exactly this — qa_answers.py → claude_answers.py → SQLite cache checked first, Claude only called for whatever's left `uncached`, with safe hardcoded fallback answers (`FALLBACK` / `PROFILE_FALLBACK`) if that call fails. The bug was never the architecture — it was that all three functions did `import anthropic` unconditionally at the top of the function, before ever checking whether anything was actually uncached. Confirmed exactly this in Raghav's own pasted log: the frame scan found the City/State/Zip inputs, and the crash happened immediately after, before any "Cache lookup" line even printed — the import itself killed the step, regardless of whether those 3 fields would have been 100% cache-resolvable (which, per this fix's own test, they are).

Fixed in all three apply scripts:
- `indeed_apply_now.py` (`smart_fill_step`): removed the top-of-function `import anthropic`; it's now imported immediately before `anthropic.Anthropic(...)` inside the existing `if uncached:` block, inside the existing `try:` that already falls through to `FALLBACK` defaults on any exception. Added a dedicated `except ImportError:` branch above the generic one so the log says "anthropic not installed — N field(s) need AI, none available" instead of a generic API-error message.
- `linkedin_apply_now.py` (`fill_and_submit_form`): same issue, plus a wrinkle — `_claude` was originally built once at the top of the OUTER function and shared via closure by two separate nested functions (the field-filler and `claude_pre_submit_review()`, the pre-submit sanity check). Simply moving the import into one of those nested functions would've left the client invisible to the other (a plain local `_claude` in one nested function isn't visible to a sibling nested function). Replaced with a memoized `_get_claude()` helper defined once at the outer function's scope — imports + builds the client on first real need, caches it, returns `None` (not a raised exception) if `anthropic` isn't installed. Both nested functions now call `_get_claude()` and check for `None` before use, each falling back to their own existing safe-default path (`PROFILE_FALLBACK` / "skip review, proceed with submit") instead of the crash the old shared top-level `_claude` variable was silently relying on `anthropic` for.
- `workday_apply_now.py` (`_smart_fill_questions`): simplest of the three — this function already had a SECOND, correctly-lazy `import anthropic` right where the client is actually built inside `if uncached:`. The top-of-function import was pure dead weight duplicating it in the wrong (unconditional) place. Just removed it.

Syntax-checked (`py_compile`) — clean on all three. Verified with two throwaway harnesses (run and discarded, not part of the pipeline): one blocked `anthropic` at import time and confirmed all three modules still import cleanly; the other directly called `indeed_apply_now.smart_fill_step()` with anthropic blocked and a City/State/Zip field set pre-seeded into qa_answers (i.e., Raghav's exact reported scenario) — confirmed it now logs "3/3 hits, 0 need Claude" and returns normally instead of crashing. Not yet confirmed against a real Indeed/LinkedIn/Workday session live.

[1.8.5] 2026-07-12 — Raghav reviewed the 1.8.4 CAPTCHA fix and pushed for a set of "pro-level" hardening items around the same code: detect-solved-by-data over pixels, an explicit state machine so the alert email only fires once per CAPTCHA, a hard timeout with a halfway reminder plus a non-blocking fallback, not stalling the whole pipeline on one CAPTCHA, reducing CAPTCHA frequency in the first place, and backgrounding email sends with retry instead of a synchronous SMTP call in the poll loop.

Checked each against what's actually in `_check_and_handle_captcha()` / `_pin_captcha_box()` / `run_all.py` / `notifier.py` before changing anything, rather than assuming the skeleton described a gap that existed:
- Solved-by-data already true: `_captcha_actually_visible()` checks the `g-recaptcha-response` token and the anchor checkbox's `aria-checked` first, authoritative, and only falls back to bframe visibility/challenge-text as a third signal (2026-07-08/09 hardening). No change needed.
- Fire-once already true: `notifier.send_captcha_alert()` is called exactly once, right when the CAPTCHA is first detected, outside the wait loop — not on every poll tick. No change needed.
- Hard timeout + skip-and-move-on already true: `CAPTCHA_TIMEOUT = 600`; on timeout the job returns `"captcha-timeout"` and lands in `_captcha_retry_queue`, retried in a dedicated pass after the main scan finishes rather than blocking it. No change needed.
- Reduced CAPTCHA frequency already true, and further along than the ask: real Google Chrome (not bundled Chromium) via a persistent profile (`launch_persistent_context` on `SESSION_DIR`), `headless=False`, `navigator.webdriver` stealth patching, and `random.uniform(...)`-jittered delays throughout, all already in place (2026-07-09 through 07-11 hardening). No change needed.
- Cross-platform blocking already mitigated: `run_all.py` runs LinkedIn/Indeed/Workday as three separate `multiprocessing.Process`es, so a stuck Indeed CAPTCHA doesn't stall LinkedIn or Workday. Not addressed here: *within* Indeed's own process the job loop is still single-threaded, so a live CAPTCHA wait does block that platform's other queued jobs for up to 10 minutes — the retry queue only helps after the timeout, not during the wait. True intra-platform concurrency (working other Indeed jobs while one tab waits on a human) is a real architectural change, not a same-size patch as the rest of this list — flagged for Raghav to decide on separately rather than bolted on here.

Two genuine gaps found and fixed:
- No halfway reminder existed — only a console-only "still waiting" print every 60s, no second email. Added a one-time (flag-guarded, not modulo-guarded) reminder email at the 5-minute mark inside the existing wait loop in `_check_and_handle_captcha()`, so a missed first alert isn't the only chance to notice.
- Email sends were fully synchronous — `send_alert()` / `send_captcha_alert()` opened `smtplib.SMTP_SSL(...)` directly inline, so a slow or flaky Gmail handshake stalled whatever loop called them (including the CAPTCHA poll loop itself). Added `notifier._send_mime_async()`: sends in a daemon background thread with up to 3 attempts / 5s between retries; `send_alert()` and `send_captcha_alert()` now build the MIME message and hand it off instead of blocking. Scoped to just these two functions (the ones actually used in the CAPTCHA flow) since every existing call site across `indeed_apply_now.py` / `linkedin_apply_now.py` / `workday_apply_now.py` / `run_all.py` already treats the return value as fire-and-forget (try/except around the call, return value unchecked) — confirmed by grep before changing the function signature's effective behavior. `notify_applied()` / `notify_session_done()` / `send_ai_interview_alert()` were left untouched — out of scope for this CAPTCHA-specific ask and some callers may check their return value for logging. Credentials were already read from `.env` via `os.environ`, never hardcoded — no change needed there either.

Syntax-checked (`py_compile`) — clean. Verified with two standalone mock harnesses (run and discarded, not part of the pipeline): one confirmed `send_alert()` returns in under a millisecond while a simulated slow+failing SMTP call takes 1.5s+5s-retry in the background, and that the retry actually re-fires; the other drove `_check_and_handle_captcha()` with a fake CAPTCHA that stays unsolved well past the 5-minute mark and confirmed exactly one halfway-reminder email fires, not one per poll tick. Not yet confirmed against a real Indeed CAPTCHA — no live browser/network access to Indeed from this environment.

[1.8.4] 2026-07-12 — Raghav asked for a review of why the Indeed Easy Apply Submit click fails even after he manually solves the reCAPTCHA image challenge himself. Traced it in `_check_and_handle_captcha()`'s `_pin_captcha_box()`: when a CAPTCHA is first detected, that function force-styles the reCAPTCHA `bframe` iframe (and every ancestor iframe between it and the top-level page) via `style.cssText` with `!important` — centered, 480px x 92vh, `z-index: 2147483647` — so a human can actually see and solve it, and re-applies this every ~3s while the challenge is still showing. Confirmed via the code's own comments (2026-07-08/09 entries) that Google hides a solved challenge by shrinking the bframe with a plain, non-`!important` style, which our earlier `!important` override always wins against — so the pinned box (still centered, still white, still top z-index) physically never goes away once applied. The post-solve auto-click already uses a real `page.mouse.click(x, y)` at the Submit button's actual bounding-box coordinates (fixed in 1.2.7/2026-07-09, not the bug here) — but a coordinate click always lands on whatever's topmost at that pixel, and the leftover pinned overlay outranks the real Submit button in z-index and sits directly over the same part of the screen. So the click was landing — just on the invisible leftover CAPTCHA box, not the button underneath it.

Fixed: `_pin_captcha_box()` now records every element handle it force-styles (the bframe itself + each ancestor iframe in the chain) into a module-level `_pinned_elements` list at the moment it styles them — no re-matching by URL substring on cleanup, which would miss ancestor wrapper iframes whose URL never contains "bframe"/"anchor" (they're plain site iframes we styled directly, not Google's own). Added `_clear_pinned_captcha_elements()`, which iterates that exact list and calls `removeAttribute('style')` on each handle (safe to fully strip rather than restore specific properties, since `_pin_captcha_box()` only ever assigns via `cssText`, replacing the whole inline style each time — there's nothing pre-existing to preserve). Called at both points `_check_and_handle_captcha()` recognizes the CAPTCHA as resolved: the `already_confirmed` early-return, and immediately before the post-solve Submit click attempt.

Syntax-checked (`py_compile`) — clean. Also verified with a standalone mock-Playwright harness (fake Frame/Element objects simulating an unsolved-then-solved reCAPTCHA, run and discarded — not part of the pipeline) confirming `_pinned_elements` is populated during pinning and fully cleared, with `removeAttribute('style')` called on every tracked handle, by the time the function returns after a solve. Not yet confirmed against a real Indeed CAPTCHA — this sandbox has no live browser/network access to Indeed. Next real run: if this was the actual cause, Submit should now register on the first auto-click right after Raghav solves the challenge instead of the click silently landing on nothing.

[1.8.3] 2026-07-12 — Raghav watched the run directly and confirmed the Create Account click has zero visible effect, and asked for an actual fix instead of more logging. Reasoning from what's already confirmed (not a new guess): Playwright's own click already passed all of its actionability checks (element visible, stable, not covered, not disabled) and threw no exception on all 4 companies — meaning the click physically lands as a real mouse event on the real button. That points away from "the click isn't landing" and toward "the click lands but we're not giving Workday's backend enough time to respond before we pile on more clicks."

The old `_submit_progressed()` only waited ~3 seconds after Method 1 (the real click) before giving up and firing Method 2 (JS click) and then Method 3 (Enter key) at what could still be a live, in-flight account-creation request — a fresh signup can involve server-side email dispatch and validation that reasonably takes longer than 3s. Firing 2-3 conflicting submit attempts within a few seconds of each other against the same in-progress request is a plausible way to end up with "nothing visibly happens" even though the first click may have been about to work on its own.

Fixed: Method 1's wait was widened from ~3s to ~10s before falling through to the rougher fallback methods. Also widened `_submit_progressed()` to treat a Workday error/toast appearing as real progress too (not just success) — if the backend answers with a rejection, that's a real response, and hammering it again with Method 2/3 would just double-submit into an already-answered request instead of surfacing what Workday actually objected to. Any such error is now captured directly into `submit_debug_*.json` from v1.8.2.

Syntax-checked (`py_compile`) — clean. This is a real behavior change (not diagnostic-only, unlike 1.8.2) grounded in what Playwright's own click already confirmed (real click, no interception, no exception) — but not yet confirmed live. Next run: if this was the actual cause, Method 1 should now succeed within its 10s window instead of falling through to 2/3. If it still fails, the submit_debug_*.json file will show whether an error appeared or truly nothing happened.

[1.8.2] 2026-07-12 — Raghav re-ran the live test after 1.8.1 and reported the same symptom: "again its not hitting the create account button," and cancelled the run. Checked the actual evidence in `screenshots/`: 1.8.1's `_submit_progressed()` guard worked exactly as designed — it correctly caught that NONE of the 4 companies tested this run (Amgen, Ryan Specialty, Vizient, NBA, all between 17:38–17:42) made real progress after any of the 3 submit methods, and correctly reported `createacct_btn_failed` instead of the old false "success." So 1.8.1 is not broken — it's just now honestly surfacing a deeper problem: the click genuinely has zero effect on all 4 different companies' forms, which is too consistent to be a per-company quirk and points at something in how these Workday-platform buttons handle the click itself.

Rather than guess again, added instrumentation directly into `workday_create_account()` so the NEXT failure explains itself without needing a live screen-watch:
- `_diagnose_button()`: right before the first click attempt, captures the button's real DOM state — bounding box, `disabled`/`aria-disabled`, computed `pointer-events`/`opacity`/`visibility`, and critically, what element is actually sitting at the button's click-point (`elementFromPoint`) and whether it's the button itself or something invisible covering it.
- A `page.on("request")` listener during the whole submit attempt, capturing every XHR/fetch call that fires — this distinguishes "the click handler never ran" (zero requests) from "the handler ran and the backend rejected it silently" (a request fired but nothing changed).
- On failure, all of this — plus the before/after page markers and the last 20 captured requests — is written to `data/crash_logs/submit_debug_<company>_<timestamp>.json`, alongside the existing failure screenshot. No new terminal output needs to be pasted; the next failure will leave a file that answers "is anything covering the button?" and "did any network call happen at all?" directly.

Syntax-checked (`py_compile`) — clean. This is diagnostic-only — no behavior changed, so the 3-method fallback and `_submit_progressed()` verification from 1.8.1 are untouched. Next step: one more `--dry-run` test, then read whatever `submit_debug_*.json` file(s) it produces.

[1.8.1] 2026-07-12 — Raghav noticed the pipeline wasn't actually hitting the "Create Account" button and asked why. Confirmed with two real screenshots (Vizient, NBA — both tagged `createacct_stuck`, produced by the false-positive guard added in 1.7.2): every field was filled correctly including Verify Password, the consent checkbox was checked, and the "Create Account" button was visibly enabled — NBA's screenshot even shows a blue focus ring around the button, proving the click physically landed on it. Yet the pipeline stayed stuck on the same page both times.

Root cause: `workday_create_account()`'s 3-method submit fallback chain (Playwright click → JS click with disabled-attr removal → Enter key) set `submitted = True` the moment a method ran without throwing an exception — never checking whether the page actually changed afterward. A click that Playwright reports as "successful" can still have zero real effect on a React app (async validation state not yet settled, an event listener attached to a different element than expected, etc.). Once method 1's no-op click set `submitted = True`, methods 2 and 3 — including the Enter-key fallback, which submits via the browser's native form path instead of depending on the button's own click handler — never got a chance to run at all. This is the same root-cause shape as the `_select_dropdown()` bug fixed in v1.7.1: success measured by "no exception," not by "did anything actually happen."

Fixed: added `_submit_progressed()`, which polls for up to ~3s after each method's attempt and only accepts it as successful if the page's marker (progress step / heading / URL path) actually changed, or the Verify Password field itself disappeared from the DOM. Each of the three methods now genuinely falls through to the next one if it didn't cause real progress, instead of stopping after the first click that merely didn't error.

Syntax-checked (`py_compile` on all six pipeline files) — clean. Reasoned directly from the two real stuck-form screenshots, not a generic guess. Not yet confirmed live — the next run should show either method 2 or method 3 actually completing the account where method 1 previously appeared to "succeed" and silently didn't.

[1.8.0] 2026-07-12 — Raghav ran a live test and hit two separate blockers, both diagnosed from real evidence (screen-shared terminal output, not guesses):

1. **Every Workday job scored 0% — nothing could ever apply, regardless of any form-filling fix.** Traced via a live `python3 -c "import claude_engine..."` check: `CLAUDE_AVAILABLE` was `False` because `import anthropic` failed — not because the package was missing, but because Raghav's `python3` resolves to Homebrew's Python (`/opt/homebrew/bin/python3`), while `anthropic` is only installed under Anaconda's (`/opt/anaconda3/lib/python3.12/site-packages`, confirmed via `pip show anthropic`). When the import fails, `claude_engine.py` silently sets `CLAUDE_AVAILABLE = False` and every scoring call quietly falls back to a hardcoded `{"score": 0}` — with zero error printed anywhere, which is exactly why a run can show "Scored: 19, Applied: 0" with no visible sign anything's wrong. This is an environment issue, not a code bug — fixed by running with `/opt/anaconda3/bin/python3` instead of bare `python3`. No code change here; flagging it because it explains why nothing has ever been applied to on Workday all session, independent of every field-filling fix in 1.6.6-1.7.3.

2. **Browser crashed on launch: `TargetClosedError: ... Target page, context or browser has been closed`.** Full traceback showed the bundled Chromium process starting (pid logged) and immediately dying, with a crashpad warning in its logs. This is the SAME error signature already root-caused and fixed for Indeed on this exact machine on 2026-07-11 (see `indeed_apply_now.py`): Playwright's bundled Chromium build is less stable here than a real, installed Google Chrome. Two fixes ported directly from that proven solution:
   - Browser launch now tries `channel="chrome"` (real installed Chrome) first, falling back to bundled Chromium only if Chrome isn't available — same dedicated profile either way, never touches Raghav's real Chrome tabs/logins.
   - Added the same PID-aware stale-`SingletonLock`-cleanup Indeed already uses: if a prior Workday session didn't exit cleanly, its lock file blocks every future launch forever; this now checks whether the lock's owning process is actually still alive before deleting it (never touches a live session's lock — only clears genuinely stale ones), and skips the run cleanly with a clear message if another Workday session really is still open, instead of crashing.

Also implemented the "save every error to one place" idea Raghav proposed earlier this session (mentioned, not yet built): wrapped `if __name__ == "__main__":` in a top-level handler that catches any crash, writes the full traceback + command + Python executable path + pipeline version to `data/crash_logs/crash_<timestamp>.log`, and tells the user to just say "check the last crash" instead of pasting terminal output — directly closes the loop that required a live screen-share to diagnose issue #2 above.

Syntax-checked (`py_compile` on all six pipeline files) — clean. Issue #2's fix and the crash-log wrapper are both new code, not yet confirmed on a live run. Issue #1 has no code fix (environment-only) — worth double-checking Raghav is consistently invoking the anaconda interpreter going forward, since this specific failure mode produces no visible error at all.

[1.7.3] 2026-07-11 — Raghav asked me to research reference projects (other open-source Workday/job-application autofill tools) and Chrome extensions in the same space, to see what a more mature implementation does differently. Read through ApplyPilot (1k stars, actively maintained) and a real Workday-specific Chrome extension, berellevy/job_app_filler (open source, 471 commits). The extension's README explained its own architecture directly: "Workday is a React site with controlled form fields... to fill a field, you have to find the correct method which updates the field's state" — confirming the Verify-Password quirk fixed in 1.7.2 isn't a one-off, it's a known class of Workday behavior. More importantly, it runs a `MutationObserver` that continuously watches the DOM and re-registers fields the instant React renders them, instead of scanning once after a fixed delay like this pipeline does everywhere.

That's a plausible root cause for a bug that's been open since Task #42 and never actually solved: Amgen's contact-information-equivalent page was misclassified as an "unknown step" — `_exists(page, WD["contact_page"], timeout=1500)` returned False on a page that visually WAS that step. A fixed `time.sleep(2)` before scanning can simply run before Workday's React app has finished attaching that page's `data-automation-id` container to the DOM.

Added `_wait_for_dom_stable(page, quiet_ms=400, timeout_ms=4000)` — waits until the page has gone quiet (no DOM mutations) for 400ms, capped at 4 seconds total so a page with constant background activity (spinners, polling widgets) can never hang a run. Wired into the main step-walking loop in `apply_to_workday_job()`, replacing the flat `time.sleep(2)` that ran immediately before every step's contact/experience/questions/voluntary/self-id/review detection check.

Syntax-checked (`py_compile` on all six pipeline files) — clean. This is a plausible, evidence-informed fix for a real open bug, not a confirmed one — the original "why is_contact returns False on Amgen" question was explicitly flagged as unresolved back in Task #42 since this sandbox has no live DOM access to inspect Amgen's actual render timing. Needs a live run to confirm it actually changes Amgen's step-detection outcome.

[1.7.2] 2026-07-11 — Raghav asked directly whether Workday is safe to run and what was still outstanding from the last run. Went back through the SAME run's screenshots that produced 1.7.1 (the Ryan Specialty/Baird "auth failed" entries at 22:36-22:38) looking specifically for why account creation kept failing for companies with no existing account, since that hadn't been root-caused yet. Found it: `fail_signin_timeout_20260711_223551.png` shows Ryan Specialty's real Create Account form with Email and Password both filled correctly, but "Verify New Password" completely empty and focused — `workday_create_account()`'s `_fill_visible()` helper calls Playwright's `.fill()` on it, which silently doesn't take on this field even though the identical approach fills the Password field directly above it. With verify-password empty, Workday's client-side validation keeps the submit button from actually completing the account, so every subsequent sign-in attempt for that company fails forever (screenshots show it looping Create Account → Sign In → Create Account).

Worse, this exposed a second, latent bug while tracing the failure path: after the forced submit click, `workday_create_account()` only treated the attempt as a failure if it found either a rendered error message or a "check your email" success phrase — if the button click silently did nothing (no error text rendered, no navigation), execution fell through all the way to the bottom and declared success anyway, saving a bogus account to `secure_store` for a company where no account actually exists on Workday's side. That would have made every future sign-in for that company fail permanently, since the pipeline would believe an account already exists and stop trying to create one.

Fixed both:

1. **`_fill_visible()` now has a keystroke fallback.** If `.fill()` doesn't actually populate the field (checked via `input_value()` immediately after), it falls back to real keyboard typing — the same trick `_fill()` already uses elsewhere in this file specifically because some Workday fields don't accept a plain programmatic value-set.

2. **`workday_create_account()` now verifies it actually left the form** before declaring success — checks that the Verify Password field is no longer present/visible. If the form is still there after the "submit," it's treated as a real failure (with a diagnostic screenshot) instead of silently saving a bogus account that would poison every future sign-in attempt for that company.

Syntax-checked (`py_compile` on all six pipeline files) — clean. Both fixes trace directly to `fail_signin_timeout_20260711_223551.png` from the run Raghav asked about — not generic hardening. Not yet confirmed live — recommend watching the next run's Create Account step specifically to confirm Verify Password actually fills now.

[1.7.1] 2026-07-11 — Raghav ran a fresh test right after 1.7.0 and reported it still hit errors and closed. Checked the actual evidence from that run (`data/workday_applied_log.json` entries at 22:34-22:38, and the real screenshots taken during it) instead of guessing. Found that on Amgen the SAME three fields as before — "How Did You Hear About Us?", "State", "Phone Device Type" — were still showing up in Workday's own "Errors Found" panel (`step_stuck_unknown-step-1_223254.png`), even though 1.7.0 added dedicated handlers for all three. No crash/traceback was found anywhere in the sandbox for this run — the last logged event is a normal (if failed) Baird sign-in attempt at 22:38:17, and the browser's session files were last touched about a minute later, consistent with the script finishing its available jobs and exiting normally rather than crashing. The "errors" Raghav saw were Workday's own validation messages and the pipeline's own "auth failed" log lines printing to the terminal — alarming to watch, but not a Python crash.

Root cause of why the three fields were STILL unfixed despite 1.7.0's dedicated handlers: `_select_dropdown()` — the function used by nearly every button-driven dropdown fill in this file (state, phone type, gender, ethnicity, veteran status, degree, etc.) — called `_click()` to find and click the button but never checked whether `_click()` actually succeeded, then typed into whatever currently had keyboard focus and unconditionally `return`ed `True` regardless of what happened. This meant `_select_dropdown()` could never actually report failure, which silently defeated every `if not filled: try a fallback selector` pattern already written in this file (state_btn → state_btn_alt in `step_contact_information()`, and the phone-device-type/state checks added in 1.6.7's unknown-step block) — they always believed the first attempt "worked" even when the target button didn't exist on the page at all, so the fallback path never ran. Confirmed directly: the before-screenshot shows "Phone Device Type" still on "Select One" immediately after the code that was supposed to set it to "Mobile" had already run and reported success.

Fixed, four ways:

1. **`_select_dropdown()` now actually verifies success** — checks `_click()`'s return value (returns `False` immediately if the button was never found) and, after typing the value and pressing Enter, re-reads the button's displayed text and only returns `True` if it changed to something real. This one fix makes every existing fallback chain in the file trustworthy for the first time.

2. **New `_select_dropdown_by_label()`** — a fallback that uses Playwright's `get_by_label()` (the real ARIA label-association algorithm: `label[for]`, `aria-label`, `aria-labelledby`, wrapping `<label>`) instead of a guessed `data-automation-id` string. Wired in wherever the ID-based attempt now correctly reports failure: `step_contact_information()`'s state/phone-type fields, and the unknown-step block's state/phone-type handling.

3. **`_smart_fill_custom_dropdowns()` now accepts Workday's own error-panel field names directly** (`error_labels` param, fed from a new `_parse_error_field_names()` that turns "Error - How Did You Hear About Us?" / "The field State is required..." into a clean field name). When Workday's error panel names a field precisely, that's ground truth — use it to find the control via `get_by_label` instead of relying on the heuristic asterisk/aria-required scan that missed it on Amgen's markup.

4. Wired `error_labels` from `_read_workday_errors()` through `_advance_page()`'s targeted retry into the dropdown resolver, so a stuck page's second attempt is driven by what Workday actually flagged, not just the same heuristic scan repeated.

Syntax-checked (`py_compile` on all six pipeline files) — clean. All four fixes trace directly to the 22:32-22:38 Amgen/Baird evidence from this run, not generic hardening. Still not confirmed with a live re-run — that's next, and this time the fallback chains should actually have teeth.

[1.7.0] 2026-07-11 — Raghav cancelled a live run and described watching it directly: the pipeline kept "entering details again and again" into the same fields, it wasn't understanding which questions actually needed a choice made vs. calling out to Claude when it couldn't figure one out, and when Workday itself threw validation errors after Next/Submit, the pipeline never looked at what those errors actually said before trying again. Three real architectural gaps, all fixed without touching auth/verification logic:

1. **Retyping the same fields on every retry loop.** `_smart_fill_questions()`'s field-extraction JS collected EVERY visible field on the page regardless of whether it already had a correct value, so every time the unknown-step block looped on a stuck page (which it does up to twice per step, and the whole step can repeat if `_advance_page` doesn't progress), it re-typed the identical answer into fields that were already filled — this is exactly what looked like "entering details again and again" on the live run Raghav watched. Fixed: text/select fields are now skipped at collection time if they already hold a real (non-placeholder) value, and radio/checkbox groups are skipped if any option is already checked. Also fixed a related latent bug found while making this change — the field-extraction query didn't exclude `type=radio`/`type=checkbox` from the generic text-field loop, so individual radio/checkbox inputs could get captured there under a mislabeled single-item entry that shadowed the correct GROUP entry (via the `seen` label dedup) and, if ever filled, would have used a no-op `el.value = ans` instead of an actual click. Excluded radio/checkbox from that loop so only the dedicated group-handling logic ever touches them.

2. **Ambiguous dropdowns being guessed instead of understood.** Workday's custom button+listbox widgets (not native `<select>`) were being handled by ad hoc, portal-specific keyword lists — e.g. "How Did You Hear About Us?" only tried matching "LinkedIn"/"Indeed"/"Job Board"/"Online" against the real options, and when none matched (confirmed on Amgen, whose actual options were things like "Company Website"/"Employee Referral"), it either stayed blank or picked whatever option happened to render first — a coin flip, not an understanding of the question. New `_smart_fill_custom_dropdowns()` generalizes this: it finds any required custom dropdown still unset, opens it to read the REAL option text, then resolves an answer through the same layered pipeline already used for text fields — curated QA answers → previously-saved Claude answers → local cache → one live Claude call that's given the actual label AND the actual scraped options (not a guess) → first-option fallback only as an absolute last resort so a required field is never left blank. Wired into the unknown-step fallback (replacing the old hearAboutUs hack entirely) and into `step_application_questions()`, both of which have job/company context available for the Claude prompt.

3. **Workday's own validation errors were never read.** After a failed Next-click, `_advance_page()` just blindly reran the exact same fill pass and clicked Next again — it never looked at Workday's own "Errors Found" panel to see what was ACTUALLY still wrong, which is exactly the panel Raghav's Amgen screenshot showed naming "How Did You Hear About Us?", "State", and "Phone Device Type" as the specific blockers. New `_read_workday_errors()` scrapes that panel (reusing the exact selector combo already proven for account-creation errors elsewhere in this file — Workday marks both auth and per-field validation errors the same way) and `_advance_page()` now prints it and runs the new custom-dropdown resolver as a TARGETED second attempt instead of a blind identical repeat. `_advance_page()` gained optional `job_title`/`company`/`jd_text` params (default empty) so steps with no ambiguous dropdowns — contact info, voluntary disclosures, self-identification, all fully deterministic already — don't need to change their call sites.

Also extracted the ~20-line candidate-profile block that gets injected into every Claude prompt into one shared `_build_profile_context()` helper (was duplicated verbatim inside `_smart_fill_questions()`; now used by both the text-field filler and the new dropdown filler so they can't silently drift out of sync).

Syntax-checked (`py_compile` on all six pipeline files) — clean. All three fixes trace directly to the live run Raghav described and the Amgen screenshots from 1.6.7 — not generic hardening. Not yet confirmed with a live end-to-end run past the point where 1.6.7 left off — that's next.

[1.6.7] 2026-07-11 — Raghav shared live Amgen screenshots after the 1.6.6 auth fix: sign-in now worked correctly (email/password both filled, correctly rejected as "no account exists yet" — expected), but the flow got stuck on the very next page anyway. Two screenshots told the story: `step_before_unknown-step-1` showed a contact-info-looking page with "Phone Device Type" still on "Select One", and `step_stuck_unknown-step-1` showed Workday's own "Errors Found" panel listing three required fields still empty/wrong — "How Did You Hear About Us?", "State", "Phone Device Type" — plus a radio group ("Have you worked at Amgen...") wrongly defaulted to "Yes", which had opened unnecessary follow-up fields. This page never got routed into the dedicated `step_contact_information()` handler at all — `is_contact = _exists(page, WD["contact_page"], timeout=1500)` returned False on it (why is still unconfirmed — no live DOM access from this sandbox to inspect Amgen's actual `data-automation-id` structure), so it fell through to the generic "unknown step" fallback, which didn't handle any of these four fields.

Four fixes, all evidenced directly by the two screenshots above, none touching auth/verification logic (per the "leave auth alone" boundary):

1. **Radio groups defaulting to the first (wrong) option.** `_fill_required_fields()`'s radio-group JS previously just clicked `grp[0]` — whatever Workday put first in the DOM — for any unanswered group. For "Have you worked at Amgen (or previously employed / current or former employee)"-style questions this landed on "Yes", which is usually false and opens a cascade of extra required fields. Added a regex (`/worked (at|for)|current or former employee|previously employed|former employee/i`) that scans up to 8 ancestor levels for the question's label/legend text and, when matched, explicitly picks the option whose associated `<label>` reads exactly "No" instead of the first DOM option.

2. **Required custom-dropdown buttons not being detected.** Item 4 of the same function only looked for a `*` inside a nearby label element within 6 ancestor levels to decide a button-driven dropdown was required. Broadened it to also treat `aria-required="true"` on the button itself as sufficient — matches Amgen's actual markup, which the label-text search apparently missed on this page.

3. **"How Did You Hear About Us?" left blank when no preferred keyword matched.** The unknown-step fallback only tried to select "LinkedIn", "Indeed", "Job Board", or "Online" — Amgen's real options ("Company Website", "Employee Referral", etc.) matched none of them, so the dropdown stayed on "Select One" and tripped Workday's required-field validation. It now falls back to clicking the first available `[role=option]` if none of the preferred keywords hit, so the field is never left blank.

4. **Phone Device Type had no handler at all in the unknown-step path.** `step_contact_information()` already sets this correctly via `_select_dropdown(page, WD["phone_type_btn"], "Mobile")`, but that function never ran on this page (see the `is_contact` misdetection above). Added the same call directly into the unknown-step fallback as a redundant safety net, so Phone Device Type gets set to "Mobile" regardless of which code path a given portal's page ends up routed through.

Syntax-checked (`py_compile workday_apply_now.py config.py indeed_apply_now.py linkedin_apply_now.py run_all.py secure_store.py`) — clean. All four fixes are reasoned directly from the two real Amgen screenshots, not generic guesses. Not yet confirmed with a live re-run past this page — that's next. The underlying question of why Amgen's contact-equivalent page isn't detected as `contact_page` remains open; hardened the fallback instead of chasing that root cause blind, since a robust fallback also protects every other portal that might hit the same gap.

[1.6.6] 2026-07-11 — Raghav watched a live run right after 1.6.5 and caught the Create Account step typing a garbage placeholder-looking email instead of his real one. Traced it to `config.py`: `CANDIDATE_NAME`, `CANDIDATE_LOCATION`, `CANDIDATE_EMAIL`, and `CANDIDATE_PHONE` were all bare `os.environ.get(key, default)` module-level assignments with no fallback to actually read `.env` if the key isn't already in the OS environment. Nothing in this pipeline ever loads `.env` into the process environment globally (no `python-dotenv`, no shell `source .env` before launching Python) — confirmed by grepping the whole codebase. So `CANDIDATE_EMAIL` was always silently evaluating to its hardcoded fallback, the literal string `"your.email@gmail.com"`, every single time the pipeline ran, regardless of the real value sitting in `.env`.

This is a bigger deal than a cosmetic typo: `cfg.CANDIDATE_EMAIL` is exactly what `ensure_workday_auth()` passes as the `email` argument to both `workday_sign_in()` and `workday_create_account()` for EVERY company, every attempt, this entire session (and, per the June 4-9 historical log data, almost certainly since before this session too). That means Workday sign-in/account-creation has been attempting with a fake, non-existent email address the whole time — a third, deeper bug underneath the two already found and fixed today (1.5.1's missing password return, 1.6.5's wrong-DOM-element fill-scope bug). All three had to be fixed before Workday auth could ever actually work.

Fix: added a small `_env(key, default)` helper to `config.py`, matching the already-correct pattern used by `get_api_key()` in the same file and `secure_store._load_env_value()` — reads `os.environ` first, falls back to manually parsing `.env` line-by-line if not found there. Applied it to all four `CANDIDATE_*` lines. `CANDIDATE_NAME` still falls back to its default ("Your Name") since it turns out `.env` never defined that key at all and nothing in the codebase actually reads `CANDIDATE_NAME` (grepped — zero other usages, so its wrong value never mattered in practice; left as-is rather than inventing a name to put in `.env` for an unused variable). `CANDIDATE_LOCATION` is similarly unused elsewhere. `CANDIDATE_EMAIL` and `CANDIDATE_PHONE` ARE both actively used (Workday auth, and as fallback answers for generic "Email Address"/"Phone Number" application questions) — verified directly: `cfg.CANDIDATE_EMAIL` now resolves to the real `your.email@example.com` and `cfg.CANDIDATE_PHONE` to the real `5555555555` instead of the placeholder/empty defaults.

Syntax-checked (`py_compile`) and verified live in a Python shell with the real `.env` present — confirmed the actual values now resolve correctly, not just reasoned about in the abstract. Still owed: a live Workday run to confirm sign-in now succeeds end-to-end with all three bugs fixed.

[1.6.5] 2026-07-11 — Raghav asked me to check what actually happened on the Workday run after the 1.6.4 checkbox fix. `data/workday_applied_log.json` showed 4 fresh attempts (Ryansg and Amgen, both a live run and a dry-run) — all `Failed | auth failed`, and `screenshots/` had 7 new `fail_signin_timeout_*.png` files between 19:40 and 20:00. Looked at two of them directly (Amgen and Ryan Specialty, two unrelated companies): both show the identical pattern — password field filled correctly (confirms the 1.5.1 password fix is working), but the Email Address field completely empty, still showing its placeholder text. Sign-in was hanging because it was trying to submit with no email at all.

Root cause was sitting right next to the comment that was supposed to prevent this exact bug: `workday_sign_in()`'s fill logic computes an ancestor "scope" by walking up from the Sign In submit button looking for a container with a password field but no Create-Account button, specifically to avoid Workday's known behavior of rendering both the Sign-In and Create-Account forms in the DOM at once (Create-Account first). But its fallback path — `scope.querySelector(...) || document.querySelector(...)` — grabs the FIRST DOM match when the scope computation doesn't pan out, and per that same rendering behavior, the first `input[data-automation-id="email"]` in DOM order belongs to the hidden Create-Account form, not the visible Sign-In one being interacted with. Confirmed this wasn't portal-specific: identical failure on both Amgen and Ryan Specialty, two unrelated Workday tenants.

Fix: replaced the ancestor-scope walk entirely with a simpler, more robust rule — fill whichever matching email/password input is actually VISIBLE (`offsetParent !== null`), instead of guessing at DOM structure. This correctly handles the dual-form-in-DOM case (the hidden form's fields have `offsetParent === null`) and also fixes single-form portals like Amgen, where the old scope-walk apparently never landed on a container that actually held the right input.

Syntax-checked (`py_compile`) — clean. Reasoned directly from two independent real failure screenshots showing the identical symptom, not a generic guess. Still not confirmed with a live re-run — that's next.

[1.6.4] 2026-07-11 — Raghav shared a live screenshot of Amgen's Workday "Create Account" page: email and password fields correctly filled, but the "I consent to the use of my personal data for the Amgen recruitment process" checkbox was still visibly unchecked. Traced it to `workday_create_account()`'s checkbox-handling block: it called `.click()` on the first visible checkbox it found and then printed "☑ Checkbox checked" UNCONDITIONALLY — it never actually verified `is_checked()` afterward, so a click that silently failed to register still got logged and treated as success. It also only handled ONE checkbox (`break`s after the first match), which would leave a second checkbox (e.g. a separate terms-of-use box on other portals) untouched if present.

Fixed: now finds every visible checkbox (`input[type="checkbox"]` and Workday's custom `[role="checkbox"]` widgets), and for each unchecked one tries three escalating methods — Playwright's `.check()` (auto-retries actionability), a plain `.click()`, then clicking the associated `<label>` element (needed on portals where the native input is visually hidden and only the label text is actually clickable) — verifying `is_checked()` after each attempt instead of assuming success. If any checkbox still can't be confirmed checked after all three methods, it now prints an honest warning instead of a false "checked" message, and saves a diagnostic screenshot (`_failure_shot`) so a future run gives visual evidence instead of another silent false-positive.

Syntax-checked (`py_compile`) — clean. Not yet confirmed live against the actual Amgen page (or any other portal) since this sandbox has no browser to test against real Workday — reasoned from the exact failure shown in the screenshot, not guessed at generically.

[1.6.3] 2026-07-11 — SAFETY WORKFLOW. Found while merging the 1.6.2 fix: `git log` showed `HEAD` sitting on a `"before: indeed: stop retrying searches..."` commit instead of a real commit of the actual Indeed fix, and `git status` still showed all the edited files as uncommitted even after running `safe_update.sh keep`. Traced it to `safe_update.sh` itself: `keep` has never actually committed the experiment branch's changes before merging — it goes straight from `git checkout main` to `git merge $CURRENT --no-ff`, so if nothing was separately committed on the experiment branch first, the merge is a silent no-op (that's what every "Already up to date." line in this session's `keep` output actually meant). The real edits were just left sitting as uncommitted working-tree changes on main, discovered only because they'd get swept up by the NEXT cycle's `start` → `snapshot.sh "before: <next task>"` call — meaning every single "before: X" commit in this repo's history actually contains the PREVIOUS task's changes, mislabeled with the description of whatever came next. Data wasn't lost (everything eventually got committed one cycle late), but the commit messages have been wrong all session, and anything left mid-cycle (like right now) was one bad `git checkout` or disk hiccup away from not being safely committed at all — directly contradicting CLAUDE.md's "Local git is the primary safety net" premise.

Also found and fixed the mirror-image bug in `discard`: `git checkout main` alone does not discard uncommitted working-tree edits when they don't conflict with main's own content, so `discard` was silently keeping every uncommitted change from a "failed" experiment instead of throwing it away — the opposite of what its name and printed message ("Your previous working code is intact") promise. Confirmed this firsthand earlier in this session: an accidental `discard` call left every pending edit fully intact rather than reverting anything.

Fixed both: `keep` now runs `git add -A && git commit -m "$CURRENT"` before checking out main and merging, so the merge always carries a real commit with a message matching the branch it came from. `discard` now runs `git reset --hard` + `git clean -fd` after switching back to main, so uncommitted edits and new untracked files from the abandoned experiment are actually gone, matching the tool's own description.

Verified both fixes in an isolated scratch git repo (not the real pipeline) before touching the real script: created a throwaway repo, ran `start` → edited a file → `keep`, confirmed a real commit now exists and `git log` shows the actual change instead of "Already up to date." Then ran `start` → edited a file + added a new untracked file → `discard`, confirmed the file reverted to its pre-experiment content and the new untracked file was gone. `bash -n safe_update.sh` syntax-checked clean.

This entry itself was committed directly (not through the now-just-fixed `keep`, to avoid being the first live use of a workflow-controlling script on itself under real conditions) — first real use of the fixed `keep`/`discard` will be the next cycle after this one.

[1.6.2] 2026-07-11 — Raghav ran the pipeline and reported Indeed "only searching back to back" and never applying. Read the actual run log (`run_20260711_192101.log`) instead of guessing: Indeed wasn't logged in, waited the full 5 minutes, then printed "❌ Indeed login timeout — skipping this run" — but then immediately started searching anyway ("🔍 Query: Data Engineer"). Every single page load failed with `TargetClosedError: Page.goto: Target page, context or browser has been closed` — the actual Chrome window had closed at some point during the 5-minute wait. It retried 3 times per page × 3 pages, failed identically every time, then moved to the next query and repeated the same futile 9 attempts — with 54 queries configured, that's up to ~40 minutes of guaranteed-failing retries against a browser that no longer exists. The log ends with a `KeyboardInterrupt` — Raghav had to manually kill it.

Two separate, compounding bugs found here, both fixed:

1. **`ensure_login()` never actually stopped the run.** Every give-up path (couldn't reach Indeed after 3 attempts, Cloudflare wall during the wait, or the 5-minute login timeout) printed a message claiming the run would be skipped, but the function has no return value and the call site (`ensure_login(page)` in `main()`) never checked anything — so `main()` always fell straight through into the full 54-query search loop regardless of whether login ever succeeded. This is a separate, real bug from the browser-closed issue — even on a run where the browser stayed open the whole time, a plain "never logged in" case would still burn a full run's worth of searching before failing every actual apply attempt at the end. Fixed by having `ensure_login()` set the existing `_indeed_blocked` global on all three give-up paths — reusing the flag rather than inventing a parallel signal, since the search loop already checks it (`if _indeed_blocked: break`) at the very top of every query iteration.

2. **The search retry loop couldn't tell "browser is gone" from "network blip."** It caught the `TargetClosedError` with a bare `except Exception`, printed a generic "search load failed" message, and retried 3 times with a 5s sleep — appropriate for a real network timeout, useless against a closed browser window, since retrying inside the same dead `page` object can never succeed. Added a new `_browser_window_closed` flag, checked for the `"Target page, context or browser has been closed"` / `"Not attached to an active page"` signature specifically (same detection strings already used elsewhere in this session for LinkedIn's equivalent crash), and if matched: stop retrying immediately, stop fetching more pages for the current query, and stop the whole query loop on the next iteration with a clear message telling Raghav to reopen the Chrome window.

Combined effect: a dead-login run now stops in seconds instead of running all 54 queries for nothing, and a closed-browser run now stops after one failed page load instead of retrying uselessly for up to 40 minutes.

Syntax-checked (`py_compile`) and traced the control flow by hand against the exact failure sequence in the real log (confirmed neither new flag is set on the normal successful-login path, and both give-up paths correctly leave `job_cards` empty without touching the dead `page` object again). Not yet confirmed with a live re-run.

[1.6.1] 2026-07-11 — Raghav said he's about to run LinkedIn + Workday. Checked `run_all.py`'s startup pre-flight (`ast.parse` syntax check on key files before any browser opens) and found `workday_apply_now.py` and `secure_store.py` weren't in the checked list — only `config.py`, `indeed_apply_now.py`, `linkedin_apply_now.py`, `resume_builder.py` were. That's exactly the gap that let this session's `workday_apply_now.py` syntax error (1.5.0 — malformed nested-quote strings from the June 20 sanitization commit) go undetected for three weeks: `run_all.py --workday-only` would have launched a browser and failed deep into a run instead of catching it instantly at startup like it does for the other platforms. Added both files to the pre-flight list.

Also confirmed for Raghav: there's no single `run_all.py` flag that runs "LinkedIn + Workday, not Indeed" together — the CLI only supports `--linkedin-only`, `--indeed-only`, `--workday-only` (each exclusive) or the full three-platform run (which skips Workday by default unless `--workday-only` is explicit). Two separate commands needed for this combination.

Syntax-checked (`py_compile`, all 6 files in the new pre-flight list individually via `ast.parse` too) — clean.

[1.6.0] 2026-07-11 — Raghav asked why LinkedIn runs take so long. Pulled the actual run data instead of guessing: the last full run took 22m54s and applied to exactly 1 job. Per-job timestamps in `data/runs/run_20260711_181442_linkedin.json` showed one single job — "Jobot — Data Analyst" — got attempted six separate times in that same run, surfacing under six different (overlapping) search queries. Every attempt failed the same way ("form exhausted without confirmation" or a 120-second stuck-field timeout) and took 2-4 minutes each — roughly 15 of the 23 minutes, about two-thirds of the entire run, spent repeatedly failing the identical job.

Root cause: `already_applied()`, the dedup check, only recognized `"Applied"`/`"Already Applied"` status — it explicitly skipped past any log entry marked `"Failed"`. So a job the pipeline structurally cannot complete (some required question it has no answer for) is never remembered as a lost cause, and gets retried from scratch every time the same job resurfaces — which happens constantly, since several of the 20 search queries ("Data Engineer Entry Level," "Junior Data Engineer," "Data Engineer New Grad") return heavily overlapping LinkedIn results.

Fix: added `_PERMANENT_FAIL_REASONS` — `"form timeout (120s)"`, `"form exhausted without confirmation"`, `"claude review blocked submission"`, plus anything starting with `"stuck on"` (covers `"stuck on Review"`, `"stuck on Next"`, etc., the dynamic variant from `fill_and_submit_form`). `already_applied()` now treats a `"Failed"` entry as dedup-worthy if its reason matches one of these — these are all genuinely structural failures where retrying re-runs the exact same code against the exact same unanswerable field, so nothing would change. Deliberately did NOT include `"Easy Apply btn not clickable"` or `"dry-run"` in the permanent list — a button-not-found could plausibly be a one-off timing/loading fluke, so those stay retryable rather than risk permanently blackballing a job over a transient glitch.

Since `already_applied()` is checked against the same `log` list that's actively appended to during a run (not just the persisted file from previous runs), this fixes both the within-run repeat (six attempts in one 23-minute session) and the across-run repeat (the same dead job resurfacing again tomorrow).

Verified with a direct simulation against the real failure shape from the Jobot job: a second occurrence of the same company+title now correctly returns `True` (skipped) when the first attempt's reason is a permanent one; a transient reason ("Easy Apply btn not clickable") correctly still returns `False` (retryable); existing "Applied" dedup behavior and unrelated-job non-matching both still behave as before. Not fixed and out of scope for this change: the URL-based half of the dedup check is separately broken for LinkedIn (`url.split("?")[0]` strips `currentJobId` along with every other query param, since LinkedIn job identity lives entirely in query string — meaning URL matching alone was always a no-op and company+title matching was doing all the real work already). Left alone since the company+title path already covers the practical case and this wasn't what Raghav asked about.

Syntax-checked (`py_compile`) and directly simulated against realistic log data (not yet confirmed with a live re-run).

[1.5.3] 2026-07-11 — SECURITY. Found while verifying the 1.5.2 encryption migration actually kept credentials out of git: tested whether the new `.enc`/`workday_accounts*.json` gitignore rules actually worked, and they didn't — neither did most of the existing "Credentials & secrets" section. Root cause: `.gitignore` does not support trailing `# comment` text on a pattern line — only a line that STARTS with `#` is a comment. Nearly every security-relevant line in this file was written as `pattern    # explanation`, e.g. `.env    # API keys...` — git treats that entire string, spaces/comment/everything, as one literal (and unmatchable) pattern, silently no-opping the rule. Confirmed with a clean A/B test in an isolated scratch repo: a bare `*.testext` pattern correctly ignored a matching file; the same pattern with a trailing comment did not.

This was NOT a bug I introduced — this formatting has been in `.gitignore` since it was written (June 20, the "sanitized for public release" commit), meaning `.env`, `*.enc`, `secure_store.key`, `raghav_profile.py`, `qa_answers.py`, `claude_answers.py`, `data/apply_log.json`, `*.docx`, `*.pdf`, and 5 other rules have been non-functional for three weeks.

Real consequence found, not just theoretical: `git ls-files` showed `raghav_profile.py` (real name/address/phone), `qa_answers.py` (salary/work-auth answers), `claude_answers.py`, `data/apply_log.json` (full application history), and `Raghavendra_Karanam_Micro1_Data_Engineer.docx` (actual resume) are all currently tracked in the local repo right now, despite being exactly the files the June 20 cleanup commit said it removed. Traced it to commit `0ead873` ("add staffing/consultancy exclusion filter") — one of the 51 commits made locally since the last GitHub push — which re-added all five in one shot, almost certainly via `snapshot.sh`'s automatic `git add`, silently let through by the broken ignore rules.

Checked exposure: confirmed `origin/main` (the actual last-pushed state on GitHub, commit `125a741`) does NOT contain any of these 5 files in its tree — so nothing has reached GitHub yet. But local main is 51 commits ahead of origin and unpushed, and this conversation itself has twice suggested `snapshot.sh --push` as a next step — had Raghav run that, all 5 files' real content would have gone to the actual GitHub remote (`github.com/raghava0071/job-pipeline`). Confirmed `.env` itself (API keys, security question answers, Workday password) was never committed at any point in history — that one's clean.

Fixed the `.gitignore` file itself first: rewrote every affected line so explanatory text sits on its own comment line above the pattern instead of trailing it. Verified every previously-broken pattern now actually matches (`.env`, `secure_store.key`, `*.docx`, `*.pdf`, `*.pma`, `*.enc`, `workday_accounts*.json` all confirmed ignored via `git check-ignore` in a live test). Then ran `git rm --cached` on the 5 currently-tracked files — removes them from git going forward without touching the files on disk (verified all 5 still present and untouched after).

NOT done, needs Raghav's decision: the 5 files' real content is still sitting in local git history (the 51 unpushed commits, plus older pushed history before the June 20 cleanup). Untracking stops it from being part of any NEW commit, but does not erase old commits — a `git push` right now would still push whatever history already exists locally. Options: (a) leave local history as-is since nothing has reached GitHub yet and this repo is presumably private/personal — untracking prevents it from getting worse; (b) rewrite local history (e.g. `git filter-repo`) to strip these files from all 51 unpushed commits before ever pushing — bigger, riskier operation, changes commit hashes, not done without explicit sign-off. Flagging this clearly rather than picking for him.

Syntax-checked (`py_compile`) — n/a, this is a git-configuration and repo-hygiene fix, not application code. Verified directly with `git check-ignore` and `git ls-files` against the real repo, not just code review.

[1.5.2] 2026-07-11 — Follow-up to 1.5.1: Raghav said to fix everything flagged, including the plaintext-password storage issue raised but left open last entry. `secure_store.py`'s own header comment always claimed Workday credentials were stored encrypted, but `load_accounts()`/`save_accounts()` actually read and wrote plain JSON (`data/workday_accounts.json`) directly — the Fernet/AES encryption code existed but was only ever invoked for a one-way DOWNGRADE migration (decrypt an old .enc file → write it out as plaintext), the opposite of what the module was supposed to do.

Fixed by flipping the priority: `save_accounts()` now writes to the encrypted `.enc` file only: `load_accounts()` reads from it. Real password data (`Raghava@2025!`) never touches a plaintext file on a normal save/load cycle going forward.

Migrating the existing data turned up a second wrinkle worth recording: a STALE legacy `workday_accounts.enc` file already existed on disk (dated Jun 6, 12 accounts) sitting alongside the newer plaintext file (dated Jun 9, 28 accounts) — leftover from before the plaintext-primary bug took over. A naive "prefer encrypted if it exists" migration would have silently reverted to the older, incomplete 12-account snapshot and lost the 16 accounts (`adobe`, `aia`, `barclays`, `boeing`, `brownadvisory`, `cadence`, `capitalone`, `cmu`, `cni`, `modmed`, `northeastern`, `onemagnify`, `roche`, `transunion`, `tsys`, `zillow`) that only existed in the plaintext file. Checked the actual key sets before writing any migration logic — confirmed plaintext was a strict superset of the old `.enc` file — so `load_accounts()` now merges both sources on first run (plaintext wins on any conflict, since it was the actively-written file), re-encrypts the merged 28-account result, and archives the old plaintext file to `workday_accounts_plaintext_backup.json` rather than deleting it.

Ran the actual migration against the real data in this session (not just code review): confirmed 28/28 accounts merged correctly, spot-checked one decrypted entry (`ag` — portal URL, email, and password all intact and correct), and confirmed the migration is idempotent (re-running `load_accounts()` afterward just reads the encrypted file, no further changes). Also caught and fixed a real gitignore gap while doing this: the new archived filename (`workday_accounts_plaintext_backup.json`) didn't match the old exact-string `workday_accounts.json` gitignore rule, which would have let a plaintext credentials file slip into a future `git add .` — widened the rule to `workday_accounts*.json`.

Scope check: this only changes where/how credentials are stored at rest. Did not touch sign-in, account-creation, or the email-verification flow — same boundary held since 1.5.0.

Syntax-checked (`py_compile`, whole repo) — clean. Migration verified directly against the real `data/workday_accounts.json` in this session (28 accounts confirmed round-tripped correctly through encrypt → decrypt). Live Workday sign-in behavior still needs a real run on Raghav's machine to confirm end-to-end — this sandbox still has no browser to test against actual Workday portals.

[1.5.1] 2026-07-11 — Raghav asked how account creation/password saving actually works on Workday, which prompted a closer re-read of `ensure_workday_auth()` and turned up a real, high-impact bug: `_get_wd_password()` reads the password correctly into a local variable (from `WORKDAY_PASSWORD` in `.env`, falling back to a direct file parse) but was missing its `return pwd` statement — so every call site got back `None` regardless of what was actually stored. That `None` gets passed straight into both `workday_sign_in()` and `workday_create_account()`, which fill it into the password field via `page.evaluate()`/`el.fill()`. Practically, that means every sign-in and every account-creation attempt, on every company portal, all session, has been running with no real password at all — likely the true dominant cause of the 37 "auth failed" log entries, more fundamental than the email-verification gate flagged in 1.5.0.

This is a one-line plumbing fix (added the missing `return pwd`), not a change to the email-verification/OTP handling — `handle_intervention()`, `workday_create_account()`'s flow, and everything downstream of getting a real password are untouched, consistent with Raghav's earlier instruction to leave the verification gate alone.

Also surfaced but NOT fixed in this pass, flagged for a separate decision: `secure_store.py`'s header comment claims Workday credentials are stored encrypted ("plaintext passwords are never written to disk unprotected"), but the actual `save_account()`/`load_accounts()` functions read and write plain JSON (`data/workday_accounts.json`) — the Fernet encryption code exists but is only invoked for a one-time legacy-file migration, never for ongoing saves. Passwords are currently sitting on disk as plain text despite the docstring. Left alone since fixing it is a bigger, separate change (would need to re-encrypt existing stored accounts, not just fix a return statement) — Raghav's "fix all" was in response to the one bug flagged in this conversation turn, not this second issue.

Verified with `python -c "import workday_apply_now as wd; wd._get_wd_password()"` — now returns a string (empty in this sandbox since no `.env` is mounted here, but confirmed it's no longer `None`) instead of silently returning nothing. `python -m py_compile *.py` clean across the repo. Live confirmation that Workday sign-in actually succeeds now still needs a real run on Raghav's machine — this sandbox has no browser to test against real Workday portals.

[1.5.0] 2026-07-11 — Raghav asked to work on Workday next. Before touching anything, reviewed the existing `workday_apply_now.py` (2,677 lines, already a from-scratch rewrite based on two reference GitHub projects) and the real data in `data/workday_applied_log.json`: 0 Applied out of 137 attempts (131 Failed, 6 Dry-Run), all from June 4–9 — it's been paused in `run_all.py` since, with a comment blaming "0/116 success rate, email verification loops."

Also pulled Workday's actual Terms of Service (workday.com/legal/site-terms.html), which explicitly prohibit "any data mining, robots or similar data gathering or extraction methods designed to scrape or extract data," developing "any applications that interact with our Sites without our prior written consent," and doing "anything that might discover source code or bypass or circumvent measures employed to prevent or limit access" — stricter, more explicit language than Indeed's or LinkedIn's. Each Workday job lives on a separate per-company tenant (wd1, wd3, wd108, etc.) requiring a brand-new account, gated behind an email-verification/OTP step that's very likely a deliberate anti-bot control, not a UX accident.

Broke the 131 failures down by reason before deciding anything: 52 "no Apply button found" (28 of those are Philips/NC/GM/Maersk, already in `BLOCKED_COMPANIES`; the rest spread across Visa, Lseg, Tel, Iqvia, Pluralsight, PwC), 40 "form walk ended" (stuck mid-form, spread thin across 18 different companies — not one systemic bug), 37 "auth failed" (the bucket that actually touches account creation/verification), 6 browser-closed crashes, 2 locked accounts. Presented this to Raghav with the same standing principle already applied to the Indeed Cloudflare block: willing to fix ordinary automation robustness, not willing to build anything whose actual purpose is defeating Workday's email-verification gate. Raghav chose to fix the mechanical bugs only (~92 of 131 failures) and explicitly leave auth/verification untouched.

**Found something bigger while reading the code for that: the file didn't even compile.** `python -m py_compile workday_apply_now.py` failed immediately — a June 20 commit ("sanitized config for public release," 3db4941) that moved personal data to `os.environ.get()` calls had botched 4 lines, wrapping real address/phone literals in malformed nested-quote syntax like `"os.environ.get("HOME_ADDRESS","") "` (a string literal containing an unescaped `"` — hard `SyntaxError`, not a runtime issue). This means Workday has been completely unable to run since June 20, three weeks before the "paused" note even mentions — the 0/137 log data actually predates this break entirely. Confirmed the same corruption isn't present in any other file in the repo (`indeed_apply_now.py`, `linkedin_apply_now.py`, `config.py`, etc. all compile clean) — scoped to these 4 lines only.

Fixed all 4: the two `_fill()` calls in `step_contact_information()` (address line 1, phone number) now pull from `raghav_profile.COMMON_QA` (already the correct source for this data elsewhere in the same file — has the real street address, not just city/state like the `.env` `HOME_ADDRESS` var would've given, which was never even the right field for a street-address box). The Claude-context f-string in `_smart_fill_questions()` had the same broken text sitting as dead literal characters inside the prompt (not a syntax error there, since it's inside a triple-quoted string, but Claude was literally seeing the text "os.environ.get(...)" as the candidate's address) — now interpolates the real address from `_CQA`. The `PROFILE_FALLBACK` dict's phone entry gets the same hardcoded-literal treatment as its neighboring city/state/zip entries for consistency. `python -m py_compile *.py` now passes clean across the whole repo.

Then made the two mechanical fixes Raghav approved. First, a real race condition in `_fill_required_fields()`'s custom-dropdown-widget filler: it called `setTimeout(...)` inside a plain (non-async) JS function passed to `page.evaluate()` — the function returned before the timeout ever fired, so `out.customDropdowns` always under-reported and, more importantly, Workday's custom button-driven dropdowns (used heavily for "Country," "How Did You Hear About Us," state pickers, and single-select application questions) frequently never actually got their option clicked before the code checked whether required fields were still empty and pressed Next. Made the function `async` and awaited the dropdown-open before reading the option list — this is a plausible real contributor to several of the 40 "form walk ended" cases, though it can't be proven without a fresh live run.

Second, `click_workday_apply()`: added `_dismiss_cookie_banner()` (checks known OneTrust/Cookiebot selectors plus a text-based "Accept All/Accept Cookies/I Agree" fallback) and run it before hunting for the Apply button, since large corporate portals (Visa, PwC, Iqvia, Lseg — all in the "no Apply button" failure list) commonly run consent overlays that sit on top of the real page. Also changed the button search from a single pass to two passes — if nothing matches on the first pass, dismiss any late-appearing banner, wait a few seconds for slow-hydrating pages, and try again — instead of giving up immediately. Added a failure screenshot (`_failure_shot`) at the "no Apply button found" return site, since this was previously the single largest failure bucket with zero visual diagnostics saved — future runs will show whether it's actually a missing selector or a blocked/challenge page.

Honest scope and limits: the syntax fix is confirmed correct (compiles clean, values now trace to the same profile data used successfully elsewhere in the file). The two mechanical fixes are reasoned from real code-level bugs matching the failure pattern, not confirmed against a live Workday run — this sandbox has no GUI/browser dependencies available to run an actual `--dry-run` here, so that step is still owed on Raghav's machine before a live run. Deliberately did not touch `ensure_workday_auth()`, `workday_create_account()`, or the OTP/email-verification handling in `handle_intervention()` — the 37 "auth failed" cases remain exactly as they were, per Raghav's explicit choice.

Syntax-checked (`py_compile`, whole repo) — clean. Live dry-run against real Workday portals not yet run.

[1.4.0] 2026-07-11 — Biggest change of the Cloudflare investigation. Raghav confirmed his REGULAR Chrome browser reaches Indeed fine on the same network, right after a run where even a freshly-reset pipeline session (new cookies, same IP) got Cloudflare-blocked immediately, both at login-check and during searches (3 confirmed blocks, correctly self-stopped per 1.3.7/1.3.8's fixes). That combination rules out both explanations tried so far: stale cookies (fresh profile still failed) and IP reputation (regular browser on the same IP works fine). The one remaining difference is the browser build itself.

Playwright's `chromium.launch_persistent_context()` by default launches its own bundled "Chromium" build, not the real, installed Google Chrome — these differ in ways deeper fingerprinting can detect (missing proprietary components like Widevine, different internal build/version signatures) that the navigator.webdriver-level stealth patch from v1.3.0 doesn't address, since that only patches JS-visible properties, not the underlying binary.

Fix: added `channel="chrome"` to the Indeed launch call, so it now uses the real Google Chrome binary — the same one Raghav's regular browsing already uses successfully — while keeping a completely separate, dedicated profile (`SESSION_DIR`) that never touches his personal Chrome session, tabs, or logins. Falls back to the bundled Chromium automatically if Chrome isn't installed on the machine, so this can't hard-break the run if unavailable.

Explicitly considered and rejected the more invasive version of this idea (attaching Playwright to Raghav's actual already-running, live Chrome via CDP) — that would mean sharing his personal browsing session with the automation (visible tab creation/closing, can't use that window for anything else mid-run) for likely the same fingerprint benefit this simpler change already gets. Kept as a documented fallback option if this doesn't fully resolve it.

Separately, fixed a real crash found in the same investigation: a LinkedIn run that successfully applied to a job then died with an uncaught `TargetClosedError` (traceback all the way to `main()`) when `click_easy_apply()` hit a closed browser page — killing the entire remaining run instead of just skipping that one job. Every other browser-closed-prone call in that same per-job loop (card click, `extract_right_panel`) already had a reopen-and-continue recovery pattern; this one call was just missed. Added the same established pattern.

Syntax-checked (`py_compile`, both files) and pre-flight (26/26). The Chrome-channel switch is reasoned from strong circumstantial evidence (browser build is the only remaining unexplained variable), not confirmed by a live re-test yet — needs an actual Indeed run to know whether it resolves the block.

[1.3.8] 2026-07-11 — Follow-up to 1.3.7's fix: Raghav asked to also widen the recheck interval itself in `ensure_login()`'s login-wait loop, from every 5 seconds to every 1 minute, to further cut down on requests while waiting (independent of the new Cloudflare-detection stop added in 1.3.7 — this reduces load even in the ordinary "just waiting for you to log in" case).

Made both the interval and the total wait budget config-driven (`INDEED_LOGIN_WAIT_INTERVAL_SEC = 60`, `INDEED_LOGIN_WAIT_TOTAL_SEC = 300`) rather than hardcoding new magic numbers, so future tuning doesn't need another code edit. Total wait budget kept at 5 minutes, just spread across 5 checks instead of 60. Updated the progress print to match (was throttled to print every 12th iteration for the old 5s cadence; now prints every iteration since each one is already 60s apart).

Syntax-checked (`py_compile`) and pre-flight (26/26) only.

[1.3.7] 2026-07-11 — Raghav asked directly whether the pipeline keeps auto-retrying while stuck on Cloudflare's "are you a robot" page. Traced it: yes, and found a real gap while answering. `ensure_login()`'s login-wait loop reloads `indeed.com` every 5 seconds for up to 5 minutes (60 attempts) whenever it doesn't see a login indicator — but it has NO awareness of Cloudflare at all, unlike the separate `_is_cloudflare_page()` check already used in the search loop. A Cloudflare block page looks identical to "just not logged in yet" from this loop's point of view, so it was blindly re-requesting the same blocked page up to 60 times before giving up. That's up to 60 extra requests against an already-flagged session every single time the pipeline gets started while blocked — directly compounding this week's Cloudflare problem, and very plausibly part of why yesterday's block was still active 30+ hours later (this loop likely fired during earlier runs this week without anyone noticing, since it looks identical to a normal "waiting for login" message in the log).

Fix: added a check for Cloudflare's own block-page signals ("additional verification required", "ray id", "verify you are human") right after each reload. If any are present, it now stops immediately with a clear message instead of continuing to hammer for the full 5 minutes.

Syntax-checked (`py_compile`) and pre-flight (26/26) only.

[1.3.6] 2026-07-10 — Raghav asked why big companies like TCS and Infosys never show up as applied. Answer: they were deliberately excluded by the staffing/consultancy filter, listed under "global IT-services / body-shop-style firms." Asked whether to unblock them and confirmed: yes, the big well-known Indian IT majors specifically (TCS, Infosys, Wipro, Cognizant, HCL, Tech Mahindra), US-based roles only — smaller/less-known IT-services firms (Capgemini, Mindtree, Persistent, Zensar, etc.) stay excluded, not asked to unblock those.

Removed the 6 majors' names from `STAFFING_CONSULTANCY_COMPANY_WORDS`, then tested — and caught a real collision before shipping: "Tata Consultancy Services" still got blocked, not by its own name, but by the unrelated generic keyword "consultancy" (which needs to stay generic, to keep catching real unnamed consulting firms for everyone else). A plain keyword removal wasn't enough. Fixed properly: added `STAFFING_CONSULTANCY_EXPLICIT_ALLOW`, checked in `staffing_filter.py` BEFORE the generic keyword loop, so these 6 companies' names always win regardless of what generic-sounding word their official name happens to contain.

Verified US-only enforcement is unaffected by this change — it's a separate, independent check (`FAKE_JOB_LOCATION_SIGNALS` containing "india", inside `is_fake_job()`), which runs before the staffing check either way, so unblocking these companies does not open the door to India-based postings from them.

Tested 19 cases directly (11 that should now pass — TCS, "Tata Consultancy Services (TCS)", Infosys, Infosys BPM, Wipro, Wipro Limited, Cognizant, Cognizant Technology Solutions, HCL Technologies, HCLTech, Tech Mahindra — and 8 that should still block — Capgemini, Mindtree, Kforce, Akraya, Sharp Decisions, a generic "IT Consulting LLC", Deloitte, McKinsey) — all 19 matched expected behavior. Syntax-checked (`py_compile`, all 4 touched files) and pre-flight (26/26).

[1.3.5] 2026-07-10 — Raghav said he's still seeing "fake companies" get applied to on LinkedIn. Pulled the actual applied-companies data (671 unique companies) instead of guessing, and the top of the list by frequency was dominated by staffing/recruiting agencies — Kforce, Insight Global, Brooksource, Talent Groups, Tech Consulting, ContractStaffingRecruiters.com, and more. Worth separating two different things that both get called "fake" in conversation: actual scam/fraud postings (a separate filter, `is_fake_job`, unaffected by this) vs. legitimate staffing agencies that Raghav has said he doesn't want regardless of legitimacy (`staffing_filter.py`, a hardcoded keyword blocklist).

Checked when the staffing filter was actually added (git log: 2026-07-02) and cross-referenced against the timestamps of the staffing-agency applications found. Almost all of them (Kforce, Insight Global, Brooksource, Talent Groups, Tech Consulting, SGA, ContractStaffingRecruiters.com — dated June 8 through June 27) predate the filter entirely; expected, not a current bug.

But two did NOT predate it: **Akraya, Inc.** and **Sharp Decisions** both got applied to on 2026-07-09 — a full week after the filter existed. Root cause: it's a fixed keyword list, and neither company's name contains a generic staffing-related word ("staffing," "consulting," etc.) or was on the named-agency list — so a real, currently-active staffing agency can still slip through indefinitely until someone notices and adds it by name. This is an inherent limitation of a keyword-list approach, not a one-off bug.

Fix: added the two confirmed live leaks (`akraya`, `sharp decisions`) plus five more agencies confirmed as real from the same applied-companies audit (`brooksource`, `talent groups`, `akkodis`, `software guidance & assistance`, `harrison clarke`) — pre-emptively blocking them since they'll keep resurfacing in future searches otherwise. Verified all 7 correctly now return `is_staffing_or_consultancy() == True`, and a plain direct-employer name still passes clean (no false positive introduced).

Honest limitation, not fixed today: this is fundamentally reactive — any staffing agency not yet on the list will keep getting through until it's noticed in the applied log and added by name. A more scalable approach (e.g. a Claude-based classifier judging "staffing agency vs. direct employer" from the job description, similar to the existing grey-zone trust-score legitimacy check) would close this permanently but costs an API call per borderline job — not built today, flagged as a possible follow-up if Raghav wants it.

Syntax-checked (`py_compile`) and pre-flight (26/26) only.

[1.3.4] 2026-07-10 — Follow-up to 1.3.3's data pull: Raghav decided to act on the finding directly, without waiting for response-tracking data — raised `LINKEDIN_FIT_THRESHOLD` from 60 back to 72 (config.py), reverting a prior deliberate throughput-over-quality tradeoff (the 60 floor was itself a deliberate loosening from 72, per the config's own history: "72% was too strict — only 2-3 apps per run").

This is explicitly a bet, not a confirmed fix — flagged clearly in both this entry and the config comment. There's still no real employer-response data to confirm raising the floor actually improves response rates; it only removes the 230 weakest-matched applications (65-69% band) going forward. Expect meaningfully fewer LinkedIn applications per run as a direct, known consequence. Revisit if throughput drops too far, or once real response-rate data exists to check whether this assumption held.

Syntax-checked (`py_compile`) and pre-flight (26/26) only — this is a config value change, not new logic.

[1.3.3] 2026-07-10 — Raghav asked why LinkedIn applications aren't getting responses. Pulled actual data instead of guessing: 873 successful LinkedIn applications since May 28, but 363 attempts (28%) failed to complete — 142 "form exhausted without confirmation", 76 "stuck on Review", 56 "stuck on Next", 52 "form timeout (120s)", 24 intentionally blocked by Claude's own review (that one's working as intended), plus a handful of JS errors. (Also found that actual employer-response tracking barely exists — one stale entry in `ai_interviews_pending.json` from three weeks ago — so "why no responses" can't be fully answered from data; flagged as a separate, unstarted piece of work.)

Found a live, reproducible example of one real cause in today's own debug log: a "Turing — Remote Quantitative Analyst" application got stuck on a LinkedIn privacy-consent `<select>` field ("By clicking 'Yes' you agree to our Privacy Policy...") whose resolved answer was the literal word "Agree" — but the actual dropdown options were "Yes"/"No", not "Agree"/"Disagree". Neither the exact-match nor the fuzzy word-overlap fallback could bridge "agree" to "yes" (they share no words), so the required field was never actually filled. The form kept re-showing the same unanswered question on steps 2, 3, and 4 in a row until it hit the 120-second timeout. This is the same failure class already fixed on the Indeed side back in v1.2.6 ("single-checkbox Agree widget") — just never ported to LinkedIn's `<select>`-based version of the same UI pattern.

Fix: added an affirmative-intent fallback to the LinkedIn select-fill logic — if the resolved answer clearly means "yes/agree" (checked against a small keyword set) but didn't literally match any option, it now picks whichever option represents the affirmative choice (skipping anything containing "disagree"/"no"/"decline") instead of leaving the field blank and stalling the form.

Honest scope note: this fixes ONE concrete, evidenced cause. "Form exhausted", "stuck on Review/Next", and "form timeout" are really just four different exit paths for the same underlying symptom — a required field the pipeline couldn't answer, stalling the form — so this plausibly helps across all four buckets, but I can't quantify how much of the 363 total it explains. Debug logs only retain the last 10 runs, so most of the historical failures aren't available to individually trace. Worth revisiting after a few more runs to see whether the failure rate actually drops.

Syntax-checked (`py_compile`) and pre-flight (26/26) only.

[1.3.2] 2026-07-10 — Raghav hit Indeed's own Cloudflare wall ("Additional Verification Required") and had to cancel manually since the pipeline wasn't stopping itself. Checked the three debug logs from this morning's runs (10:41, 10:56, 10:59 — three runs inside 20 minutes) to see what actually happened rather than guessing whether the new stealth patch (1.3.0) caused it.

Timeline shows this is NOT the stealth patch: the 10:41 run — the first run to carry that patch — worked fine early on (logged into Indeed, browsed search results, progressed cleanly through a full SmartApply form, steps 1-4), and only started hitting `net::ERR_ABORTED` on search page loads later in that same run. The next two runs, 10:56 and 10:59, hit full Cloudflare challenge pages from the very start. That's the standard shape of IP/session-level rate-limiting escalating from repeated automated requests — consistent with how much this session has run today (many CAPTCHAs, several cancels/restarts, three full runs back to back) — not something a single code change introduced.

Real gap found: the pipeline already has a bail-out for repeated blocking (`_consecutive_empty_queries`, 4 in a row), but it's keyed off an AMBIGUOUS signal (0 cards returned, which could have several causes) and doesn't fire on the DIRECT, unambiguous signal — `_is_cloudflare_page()` already detects a confirmed Cloudflare page, but that only led to a 45s wait + one retry + skipping that one query, then moving on to hammer the NEXT query against the same wall. With up to 3 page-loads per query, that's several minutes of continued hammering per query, for as many as 4 queries, before the old bail-out would even trigger — actively making an already-flagged session worse instead of backing off, and long enough that Raghav gave up and cancelled manually before it self-stopped.

Fix: added a separate, faster counter (`_consecutive_cf_blocks`, config `INDEED_CF_BLOCK_BAIL_THRESHOLD = 2`) that reacts to CONFIRMED Cloudflare blocks specifically — 2 in a row now stops the Indeed run immediately (same `_indeed_blocked` mechanism already used for CAPTCHA-cooldown and low-yield bails) and sends an alert, instead of waiting for the slower, ambiguous 4-empty-query threshold.

Important: this doesn't un-block anything already in effect. Cloudflare/Indeed rate-limit blocks like this typically need real time to cool down — running again immediately will likely just hit the same wall right away. Recommend waiting a few hours (ideally longer, or overnight) before the next run.

Syntax-checked (`py_compile`) and pre-flight (26/26) only.

[1.3.1] 2026-07-10 — Raghav cancelled a run mid-CAPTCHA again: a "select all squares with traffic lights" challenge (a 4x4 grid — different and taller than the 3x3 "select all images with X" variant this was tuned against) got cut off at the bottom, Verify button unreachable, same failure class as before.

Two real causes, not one: (1) the pin/fullscreen-ancestor logic only ever ran ONCE, at the moment a CAPTCHA was first detected. If Google serves a retry or a different, taller challenge variant afterward — which is exactly what a "select all squares" grid appearing where a "select all images" grid was before implies — or renders into a fresh bframe element, none of that later content was ever re-pinned; whatever got styled at t=0 was all it ever got. (2) the height budget (85vh, capped 820px) was sized for the 3x3 grid's natural height and didn't leave room for a 4x4 grid's extra row.

Fix: extracted the pin/fullscreen-ancestor logic (previously a one-shot inline block) into a reusable `_pin_captcha_box()` function, called once at detection as before AND again every ~3 seconds throughout the 10-minute wait loop while a challenge is still showing — cheap (idempotent CSS, no-op if nothing changed) and self-heals against any later DOM change instead of assuming the challenge's shape never changes after the first render. Also widened the height/width budget (92vh, capped 960px, width 460→480px) to fit taller grid variants.

Syntax-checked (`py_compile`) and pre-flight (26/26) only — reasoned from a screenshot and the mechanics of how reCAPTCHA serves retries/variants, not confirmed against a live multi-variant CAPTCHA sequence.

[1.3.0] 2026-07-09 — Raghav asked how to improve/customize the pipeline to actually solve the recurring CAPTCHA problem, not just keep patching it after it appears. Researched how established open-source job-apply bots handle this (GodsScion/Auto_job_applier_linkedIn, 2.4k stars, most active project found) and general reCAPTCHA-bypass literature. The consistent finding: nobody serious tries to auto-solve reCAPTCHA v2 image challenges (it uses behavioral risk analysis specifically to resist that) — the established projects instead reduce how often it triggers in the first place, via `undetected-chromedriver`-style fingerprint patching.

Checked what this pipeline was doing differently: it only passes `--disable-blink-features=AutomationControlled` at launch. That flag does NOT clear `navigator.webdriver` in current Chromium — it stays `true` by default, and it's one of the single most common, deterministic bot signals a site can check (reCAPTCHA Enterprise's risk scoring reads it directly). No other automation fingerprints were patched either (plugins list, languages, `window.chrome.runtime`, the `navigator.permissions.query` behavior difference Playwright's default context exhibits vs a real browser).

Added a context-level stealth init script (`context.add_init_script(...)`, runs before every page/frame's own scripts, so it covers every navigation for the life of the browser, not just the first page) to both `indeed_apply_now.py` and `linkedin_apply_now.py` at their `launch_persistent_context` call sites. Patches: `navigator.webdriver` → undefined, `window.chrome.runtime` presence, `navigator.languages`, `navigator.plugins` (non-empty), and the notifications-permission query quirk. This is the same category of fix undetected-chromedriver applies under the hood for the most-starred reference project.

Deliberately did NOT touch the field-filling logic (`.fill()` calls, which inject values instantly rather than typing character-by-character — also a known bot signal, also called out in the research). That logic has been through many iterations this session already and is fragile; changing it risks reopening bugs already fixed (radio/checkbox handling, React controlled-input quirks, hash-labeled field caching). Flagged as an optional follow-up for Raghav to decide on, not bundled into this change, per the project's own rule to write only what's needed.

Syntax-checked (`py_compile` both files) and pre-flight (26/26) only — reduced CAPTCHA frequency is inherently a statistical claim (fewer challenges over many runs), not something a single run can confirm or deny either way.

[1.2.21] 2026-07-09 — Raghav pushed back hard, correctly: the two screenshots he sent were from the SAME session, one confirmed pre-1.2.20 (well-centered, Verify visible) and one confirmed running 1.2.20 (broken again — box cut off bottom-right, same failure mode as before). So the pixel-offset fix from 1.2.20 didn't hold up live. Rather than patch that arithmetic a third time, switched to a structurally different approach that removes the arithmetic entirely.

Why the offset math was fragile even though the reasoning behind it was sound: it depended on correctly identifying which single frame is "the" nesting ancestor (fragile if Indeed's actual nesting is one level in some flows and two-plus in others — the code only ever walked up one level via a `"smartapply.indeed.com" in url` string match), on reading the top-level window size at exactly the right moment (a later `page.set_viewport_size()` call could still invalidate it), and on `bounding_box()` correctly reflecting current scroll/layout at the instant it's read. Any one of those being off by even a little reproduces exactly what Raghav saw: a box that's roughly right but shifted enough to clip the Verify button off-window.

New approach, reasoned from first principles about *why* `top:50%; left:50%` ever failed at all: it's only wrong because the bframe's containing block (its nearest ancestor iframe's own rendered box) isn't the same size as the real browser window. Instead of computing where the real window's center falls inside that mismatched box, make the containing block ITSELF the size of the real window first. Walk every ancestor iframe between the bframe and the top-level page via Playwright's `frame.parent_frame` — however many levels there happen to be, one or several, doesn't need to be known or guessed in advance — and force each one to `position:fixed; inset:0; width:100vw; height:100vh`, turning every iframe in the chain into a fullscreen overlay of its own parent document. Once every ancestor is fullscreen, the bframe's containing block genuinely IS the real window, and the original simple 50%/50% centering trick (the one proven to work in the 2026-06-22 code) becomes correct again — no coordinates to compute, no assumptions about nesting depth baked in, and no `page.main_frame.evaluate()` viewport read that a later resize call could invalidate.

Syntax-checked (`py_compile`), pre-flight (26/26), and confirmed `Frame.parent_frame` is a real Playwright API (checked directly against the installed library) — but this is still reasoned from screenshots, not a live CAPTCHA run. If this also doesn't hold, the next thing to check is whether the ancestor walk is actually finding all the right frames (add a debug print of the ancestor chain URLs so the next failure is diagnosable from the log instead of another screenshot round-trip).

[1.2.20] 2026-07-09 — Raghav sent a live screenshot mid-CAPTCHA: the pinned box was sitting bottom-right, partly cut off past the actual window edge, not centered at all. He also asked directly whether this used to work "10 weeks back" and whether I had access to that old code. Git history only goes back to 2026-05-31, but the 2026-06-22 version (last one before this regression) was available, so I diffed it against today's code instead of guessing.

Found the real cause: the 6/22 version located the CAPTCHA with a plain top-level `document.querySelectorAll('iframe')` — meaning back then Google's `bframe` challenge iframe was a DIRECT CHILD of the top-level Indeed page. `position: fixed; top/left: 50%` correctly centers against the true browser window when there's no iframe nesting in the way, which is why the old "reshape and center" behaviour Raghav remembered actually worked. Indeed has since restructured SmartApply to embed the whole apply form — CAPTCHA included — inside its own nested iframe. That silently broke the old top-level search completely (confirmed back on 7/8: zero detections across three full logs), which is why detection got rewritten to walk `page.frames` directly. But the CENTERING math was never updated for the same nesting change: `position: fixed` percentages are always relative to the CONTAINING BLOCK's own rendered viewport — for an element living inside a nested iframe, that's the iframe's own box, not the real window. So "50%, 50%" was centering the CAPTCHA within the SmartApply iframe's own (smaller, offset) area the whole time, not the actual screen — it only ever looked centered by coincidence, when that iframe happened to fill the visible window.

Fix: replaced the percentage-based centering with real pixel coordinates. Read the true top-level window size (`window.innerWidth/innerHeight` from `page.main_frame`) and the SmartApply iframe's own on-screen offset (via `frame_element().bounding_box()`, which already accounts for outer-page scroll), then translate "center of the real window" into the SmartApply iframe's local coordinate space by subtracting that offset, and apply the result as absolute `left/top: Npx` — no `%`, no `transform`. This stays correct regardless of where Indeed positions the SmartApply iframe on the page, or how tall its internal layout is, unlike the old percentage approach.

Syntax-checked (`py_compile`) and pre-flight (26/26) only. This is a coordinate-math fix reasoned from a live screenshot and a direct diff against known-working code, not something I can verify without a live CAPTCHA — needs a real run to confirm the box now lands inside the visible window. Also note: like the two fixes before it today, this only takes effect on the next fresh run — it can't help a CAPTCHA that's already stuck in an already-running process.

[1.2.19] 2026-07-09 — Raghav reported the actual live symptom, mid-run: he solved the CAPTCHA and submitted, but the pipeline "still sitting in that page," not registering the solve at all, on top of the already-fixed frame-click issue from 1.2.18. Read `_captcha_actually_visible()` line by line rather than guessing again, since it's called both to detect a CAPTCHA and, every second, inside the 10-minute wait loop to decide whether it's been solved.

Found two compounding bugs making the "still showing" check effectively permanent once a challenge had ever rendered on a page:
1. One of its three OR'd signals matched the bare substring `'recaptcha'` inside the bframe's own document text. That word is part of Google's permanent widget branding/footer, present whether or not the challenge is solved — so this signal could never truthfully report "resolved." The other two specific-phrase checks ('select all images', etc.) aren't safe either, since `f.evaluate()` reads a frame's document regardless of whether that frame is visually hidden, and reCAPTCHA typically hides a solved challenge by collapsing its container rather than clearing the DOM text inside it.
2. The pin/resize step (1.2.17) forces the bframe's entire inline style with `!important` on position and size, applied once at detection time. If Google's own JS later tries to shrink/hide that same box after a solve via a plain (non-`!important`) style change, our earlier `!important` values win and the box visually stays in place — so even the size-based visibility signals could get stuck reporting "still visible."

Together these explain exactly what Raghav saw: the wait loop's `still_captcha` check never had a real path to flip to `False`, so it just burned the full 10 minutes regardless of whether he'd actually solved and submitted.

Fix: added two independent, Google-controlled "solved" signals, checked first and treated as authoritative (bypassing all visibility/text heuristics entirely when present):
- The hidden `g-recaptcha-response` token — Google populates this element with a long opaque value the instant the widget considers itself solved. Read from the SAME document that embeds the widget (Indeed's own SmartApply frame, not the cross-origin anchor/bframe), so Playwright can read it directly and it's completely unaffected by our own CSS override on the bframe.
- The anchor (checkbox) iframe's own `aria-checked="true"` state, read from inside that frame's own document — we never touch the anchor frame's internals, only its z-index on the outer `<iframe>` tag, so this is also untouched by the pin logic.

Also removed the bare `'recaptcha'` substring from the fallback text-based check, since it was never a valid "still active" signal to begin with.

Syntax-checked (`py_compile`) and pre-flight (26/26) only. Important caveat for the run Raghav has going right now: this fix is in the source file but Python doesn't hot-reload a running process — the CAPTCHA wait loop that's currently stuck will keep running the OLD logic until that process exits (either it times out at 10 minutes and moves on, or he cancels it). The fix only applies starting with the next fresh run of the pipeline.

[1.2.18] 2026-07-09 — Raghav confirmed the 1.2.17 centering fix worked ("later it push the cap into the middle of the page I can easily solve and submitted the application"), then raised a separate, more concerning observation: "I noticed the pipeline already answering the cap by itself it answer wrongly... I noticed this happened in the past two." He asked whether the pipeline should be trying to solve CAPTCHAs at all.

Answer: no, it never was intentionally trying to solve them — but it was capable of accidentally interacting with one. The pipeline has several "stuck form" fallback functions that scan every frame on the page and click the first visible, enabled button whose text loosely matches a nav-like keyword (continue/next/submit/apply/review). These loops iterate `page.frames`/`pg.frames` with no awareness that one of those frames can be Google's reCAPTCHA widget (the anchor checkbox iframe or the bframe image-challenge iframe, both cross-origin under recaptcha.net/gstatic.com). The Vision-assist "stuck form" fallback fires specifically when the pipeline judges a form isn't progressing — which is exactly the situation a CAPTCHA creates. So when that fallback ran while a CAPTCHA was on screen, it could click an image tile inside the challenge or hit the challenge's own Verify button before Raghav had actually finished solving it, which would submit a wrong/incomplete answer — matching his description exactly.

Root cause confirmed by code audit (four separate frame-iteration loops, all missing any recaptcha exclusion):
- `_get_nav_buttons()` — decides which frame becomes the "active" nav frame for everything downstream; could hand back the recaptcha frame itself if challenge text ever loosely matched a nav keyword.
- `_click_any_forward_button()` — the generic stuck-form click fallback.
- `_click_submit_btn()` — both its trusted-mouse-click loop and its JS-fallback loop.
- The post-CAPTCHA-solve auto-click-Submit logic inside `_check_and_handle_captcha`'s 10-minute wait loop — both its mouse-click and JS-fallback loops.

Fix: added a new helper, `_is_recaptcha_frame(frame)`, which checks whether a frame's URL contains "recaptcha" or "gstatic.com/recaptcha", and added `if _is_recaptcha_frame(frame): continue` to all six loop sites above. Checked `_force_click_continue_on_resume_page()` separately — it already only touches frames with `"resume-m"` in the URL, so it was never able to reach the recaptcha frame and needed no change.

Syntax-checked (`py_compile`) and pre-flight (26/26) only — this closes every generic-click path I could find into the recaptcha frame, but I can't fully verify it live without reproducing a CAPTCHA and watching the pipeline not touch it. If wrong answers still get submitted after this, the next thing to check is whether Indeed ever renders the challenge in a frame whose URL doesn't contain "recaptcha"/"gstatic" (e.g. a proxied or renamed URL), which would slip past this exact-substring check.

[1.2.17] 2026-07-09 — Raghav pinpointed the exact mechanism of the CAPTCHA problem himself: the challenge's own internal Verify button renders below the visible area of the pinned box, and asked for it to be centered in the middle of the screen instead. That diagnosis exposed a real mistake in the 1.2.13 fix: `overflow-y: auto` was set on the OUTER bframe iframe element, which does nothing for this problem — it only governs how the iframe itself behaves if IT overflows the page, not whether content INSIDE that iframe (Google's own cross-origin document, a completely separate scroll context) can be scrolled into view from the parent page. There was never a way to reach a button below the fold with that approach; the only real fix is sizing the box generously enough up front that nothing gets cut off in the first place.

Rewrote the pin styling: centered both horizontally AND vertically (`top: 50%; left: 50%; transform: translate(-50%, -50%)`) per Raghav's own suggestion, and sized relative to the actual viewport (`height: 85vh`, capped at 820px, floored at 600px) instead of guessing another fixed pixel number — this is the third size iteration (330×650 → 420×780 → 460×85vh) and the first one that scales with the real window instead of a hardcoded guess, so it should stay correctly sized regardless of the exact challenge variant (3x3 vs 4x4 grid, with or without a reload row) or screen size.

Syntax-checked (`py_compile`) and pre-flight (26/26) only — needs a live CAPTCHA to confirm the Verify button is now actually reachable.


[1.2.16] 2026-07-09 — Raghav asked directly whether his full CAPTCHA requirements list had actually been addressed. Went back through the code line by line against each point instead of assuming: email alert on detection (present, notifier.send_captcha_alert), 10-minute wait then move to next job (present, CAPTCHA_TIMEOUT=600s, timed-out jobs get queued in _captcha_retry_queue and the main loop continues to the next job card — confirmed, not just assumed), poll every 15-30s (already exceeded — polls every 1s). All of that was already correct.

But found one real gap that was missed: the "auto-click Submit after the CAPTCHA is solved" step (inside the 10-minute wait loop) is a THIRD, separate click implementation that was never touched by the 1.2.7 trusted-click fix — it still used a raw JS `b.click()`, the exact untrusted-event pattern already proven unreliable against Indeed's reCAPTCHA-gated Submit button everywhere else. This runs silently in the background the moment the CAPTCHA is solved, so if it fired and failed, Raghav would see nothing except a Submit button that looks normal but won't respond — matching his exact description ("after solving the CAPTCHA I'm not able to hit submit button"). Fixed to try a real trusted `page.mouse.click()` at the button's actual coordinates first (same pattern as _click_nav/_click_submit_btn), falling back to the JS click only if no button can be located via Playwright's own locator.

Syntax-checked (`py_compile`) only. This is the most direct fix yet for the specific "can't hit submit after solving" complaint — needs a live CAPTCHA solve to confirm.


[1.2.15] 2026-07-09 — Raghav flagged that the "Applied: submitted" email for GRP Solutions Inc — Data Engineer looked wrong. Opened the actual attached screenshot directly (screenshots/indeed_grp_solutions__inc_data_engineer.png) instead of asking him to re-send it: it unambiguously shows the "Review your application" / "100%" pre-submission page — "You won't be able to edit your application after you submit," Submit never clicked. `_is_confirmed()` had fired True while still sitting on the review page and reported a false success. This is a materially worse bug than anything fixed so far this week: a false "failed" just means a wasted retry, but a false "submitted" means Raghav believes a real application went to the employer when it never did.

Root cause is almost certainly some hidden/accessibility-only text on Indeed's review page — e.g. a pre-rendered ARIA live region meant to announce success to screen readers after a real submit, whose template text is present in the DOM (and picked up by `document.body.innerText`, which doesn't reliably exclude visually-hidden-but-accessible content) even before any submission happens. Rather than trying to guess which exact hidden string it is, added a negative guard to `_is_confirmed()`: a CONFIRM_PHRASES match is now vetoed if the current URL still matches known pre-submission patterns (review-m, review-module, questions-module, resume-selection) or the page still literally shows "review your application" / "you won't be able to edit your application" text. A confirm-phrase hit on a page that's demonstrably still the review screen is now treated as a false positive instead of a success.

This very plausibly explains the ongoing CAPTCHA complaints too, not just the false-positive itself: if `_is_confirmed()` fired while Raghav was mid-CAPTCHA on the review page, the step loop would exit immediately believing the job was done — abandoning the CAPTCHA-handling flow entirely (no further polling, no pin refresh, nothing) mid-solve. That would produce exactly what he described: a CAPTCHA that seems to just stop being watched, with no path to a working Submit button, even though the underlying wait-and-poll logic (already checking every 1s, well inside his requested 15-30s) is sound on its own. Fixing the false-positive should let that existing polling loop actually run to completion instead of getting cut off.

Syntax-checked (`py_compile`) and pre-flight (26/26) only. This needs a live run to confirm both that (a) real successes still get correctly detected (the guard is scoped narrowly to known review-page markers so it shouldn't affect genuine post-submit confirmation pages, which don't carry those markers) and (b) a CAPTCHA encountered on a future run now gets to run its full detection/pin/wait flow without being cut off early.


[1.2.14] 2026-07-09 — Read a genuinely fresh run's log (started well after every prior fix landed, so no stale-process ambiguity this time) and found the real remaining gap in the CAPTCHA timing fix: the 1.2.11 version did ONE re-check, 5 seconds after the "page unchanged" early-exit triggered, then gave up if nothing showed. This run's evidence shows that wasn't long enough — a real CAPTCHA rendered even later than that single 5-second window, after the job had already been marked failed and the code had moved on to scoring/searching other jobs entirely. The tab sat open with the unsolved challenge and nothing was watching it anymore — no email, no notification, no pin — exactly matching what Raghav described live. Widened this from one 5s-then-check to a repeated watch loop: checks every 3 seconds for up to 24 seconds before finally giving up, so a challenge that renders late has real, repeated chances to be caught instead of one narrow window.

Being direct about this: this is the fourth iteration on CAPTCHA timing/detection in two days (1.2.9 wrong detection scope, 1.2.10 unreliable single detection method + broken resize, 1.2.11 detection window too short, 1.2.14 window still too short). Each was a real, evidence-based fix for what the log actually showed at the time, not a repeat guess — but there's no guarantee 24 seconds is enough either if Indeed's risk system delays the challenge even further under some condition not yet observed. If this happens again, the useful next step is watching the actual wall-clock gap between the last submit click and the CAPTCHA becoming visible on screen, since that number is what determines how long this watch window actually needs to be — a guess without that number is just another patch in the dark.

Separately, fixed an unrelated but confirmed bug in the same log: LinkedIn's post-submit screenshot step crashed with `name 're' is not defined` on every successful application (job still counted as applied — this only broke the screenshot attachment). The `re` module was used for filename-sanitizing regex but never imported at the top of linkedin_apply_now.py. Added `import re`. Trivial, low-risk, confirmed root cause.

Also clarifying for Raghav: the "Applied: submitted" email with a clean screenshot (no CAPTCHA visible) was from a DIFFERENT job than the one that got stuck — the first job that run didn't need a CAPTCHA at all and genuinely submitted cleanly; the second job is the one that hit the CAPTCHA and got abandoned per the timing bug above. Not a sign of anything being wrong with that first email.

Syntax-checked (`py_compile`) and pre-flight (26/26) only.


[1.2.13] 2026-07-08 — Raghav reported a second Indeed application where he couldn't find the CAPTCHA's Verify/Submit button after solving the image grid. Checked the running debug log first — the process he was watching had been launched before the 1.2.12 LinkedIn fix landed (confirmed: its LinkedIn section was still hitting the exact "no nav button" bug 1.2.12 fixed, meaning that process is running stale in-memory code — Python doesn't hot-reload a script that's already executing), and that same log's actual Indeed section had already completed cleanly (2 applied, 0 failed, CAPTCHA solved successfully) — so this is very likely a separate, newer run with no corresponding debug log yet, or the same browser window being looked at after the fact.

Regardless of which run it was, the underlying complaint is worth fixing on its own merits: across all three CAPTCHA screenshots sent tonight, the image grid was visibly cut off at the bottom with no Verify button in view. The pin/resize logic (1.2.10) was forcing the challenge iframe to a fixed 330×650px box — but reCAPTCHA's image-challenge widget renders at a roughly fixed intrinsic width around 400px; squeezing the outer iframe narrower than that very likely clipped its internal layout and pushed the Verify button below the visible area, independent of whether detection/pinning itself fired correctly. Widened to 420×780px, capped at `max-height: 95vh` so it can't exceed the actual screen on a smaller display, and added `overflow-y: auto` as a fallback in case content still doesn't fully fit. Low-risk, purely cosmetic/sizing change — doesn't touch detection or submission logic.

Also worth restating for Raghav directly: any pipeline run already in progress when a fix lands keeps running the OLD code in memory until it's fully stopped and restarted — editing the .py files on disk doesn't affect a process that already loaded them. Every fix from tonight only takes effect on the NEXT fresh `bash run_pipeline.sh` / `bash run_indeed_now.sh` invocation.

Syntax-checked (`py_compile`) only — needs a live run that hits a CAPTCHA, started fresh AFTER this change, to confirm the Verify button is actually visible now.


[1.2.12] 2026-07-08 — Confirmed via the debug log that the Indeed CAPTCHA fix (1.2.9-1.2.11) worked end to end: job 1 hit a real CAPTCHA, got detected, emailed, pinned, solved, and submitted; job 2 needed no CAPTCHA and submitted cleanly too — 2 applied, 0 failed for Indeed. Raghav then asked why "2 applications failed," which turned out to be LinkedIn (same run, later in the log), not Indeed — two separate, real bugs in linkedin_apply_now.py's step-navigation loop.

(1) Innova Solutions — AI Engineer: the log's own debug line proved 'Next' was visible on the page ("step 0: no nav button. visible=['Next', ...]") at the exact moment the code claimed no nav button existed and gave up immediately ("form exhausted without confirmation"). Root cause: the primary button-finder used `page.locator("button:has-text(...)").first` — locking onto the FIRST matching element in DOM order and giving up on that whole button label entirely if that one instance wasn't visible/enabled, never trying any other element with the same text. The debug-print-only query used a broader, correctly-filtered set (any non-disabled button with a real bounding box), which is why it could see 'Next' when the primary logic couldn't — the two code paths were checking different things. Fixed by replacing `.first` with `.all()` and iterating every matching element per button label, clicking the first one that's actually visible and enabled — same pattern already used successfully in indeed_apply_now.py for the analogous bug class.

(2) Textron — Data Engineer II: hit "form timeout (120s)" immediately after step 3's Next click, with zero log output in between — no way to tell whether one step silently ate ~100+ seconds (most likely explanation: Playwright's `.click()` has its own internal actionability retry, up to 30s by default, if an element isn't immediately clickable — four such near-maxed-out retries across steps 0-3 could plausibly account for it) or something else entirely. Not fixed blind — instead added elapsed-time logging at the top of every step iteration and on timeout, so the next occurrence shows exactly how long each step actually took instead of a silent jump straight to the timeout. Flagging this one honestly as unconfirmed pending that data, consistent with how every other uncertain fix this session was handled — no guessing without evidence.

Syntax-checked (`py_compile`) and pre-flight (26/26) only; needs a live LinkedIn run to confirm (1) actually stops firing and to gather real timing data for (2).


[1.2.11] 2026-07-08 — Raghav sent a third live screenshot (11:07 PM), same review-module page, same unresolved/unpinned CAPTCHA, still no email. Read the fresh debug log for the exact job this happened on and found the real timing issue: the frame scan taken right before the Submit-retry loop started shows NO bframe challenge iframe at all — only the unclicked reCAPTCHA checkbox ("anchor" frame). The visual image challenge only exists once Google's risk engine decides to escalate, which — based on this evidence — happens partway THROUGH the retry sequence, not before it. The 1.2.7 early-exit ("page unchanged for 3 attempts, stop wasting time") was firing at exactly attempt 3, which is very plausibly the same moment the challenge was in the process of rendering — so the loop stopped watching that page and moved to the next job right as a real CAPTCHA was appearing, before the next scheduled `_check_and_handle_captcha()` call would have caught it. That leaves the browser tab sitting open with a live, unsolved challenge that no code is watching anymore — matching every screenshot so far: visible, unpinned, no alert.

Fixed by adding one more explicit CAPTCHA check, with a 5-second wait first, at the exact moment the early-exit is about to fire (indeed_apply_now.py, submit-retry loop's `_same_sig_count >= 2` branch) — before declaring the job dead, it now gives a possibly-still-rendering challenge one more chance to appear and be caught by the (now-fixed, multi-signal) detection from 1.2.10. If a CAPTCHA is genuinely there, this routes into the normal alert/resize/10-minute-wait flow instead of silently giving up. This is additive and only adds a single extra check + 5s wait at the one specific point where evidence shows a challenge was rendering late — it doesn't change behavior for jobs that were never going to show a CAPTCHA at all.

This is the third iteration on CAPTCHA handling tonight (1.2.9: broken detection scope; 1.2.10: unreliable single detection method + broken resize logic; 1.2.11: detection running too early relative to when the challenge actually renders). Each one was a distinct, evidence-based bug, not a repeat of the same guess — but given three misses in a row, this needs a live run that actually reaches a CAPTCHA to confirm before calling it solved. Syntax-checked (`py_compile`) and pre-flight (26/26) only.


[1.2.10] 2026-07-08 — 1.2.9 fixed the CAPTCHA visibility check but Raghav sent a second live screenshot minutes later showing a real, active image challenge on the exact same review-module page, and the new debug log still showed zero "CAPTCHA DETECTED" prints. One detection method (frame_element() + is_visible()) wasn't reliable enough on its own — rewrote `_captcha_actually_visible()` to check three independent signals, ORed together, so a single method's blind spot can't hide a real CAPTCHA again: (a) the original frame_element()+is_visible()+bounding_box check, (b) frame.locator('body').is_visible() — Playwright's actionability engine, a genuinely different code path than (a), (c) asking the bframe's own document whether actual challenge text is present ("select all images", "click verify", "recaptcha"), evaluated from inside that frame so it can't be broken by nesting or cross-origin issues. Deliberately checked for challenge-specific TEXT in (c) rather than just "does this frame have any rendered size" — a hidden-after-solve iframe can still have a fully laid-out internal document even while invisible from the outside (hiding is usually done via the parent's CSS on the iframe tag, not by collapsing the child document), so a bare size check would have reintroduced the exact "stays true forever after solving" staleness bug this function exists to prevent.

Separately, and just as important: found the CAPTCHA-window resize/pin logic (meant to move the challenge into a fixed, fully-visible position so the Verify button is reachable — the exact feature Raghav asked about) had the identical bug as the original detection code, and was never touched by the 1.2.9 fix since it's a separate function. It also ran `document.querySelectorAll('iframe')` via page.evaluate() scoped to the top-level document only, so `bframe` was always `undefined` and it silently did nothing — while the print statement below it fired unconditionally regardless, always claiming "CAPTCHA window pinned" even when the resize never ran. This is very likely why the CAPTCHA in both of Raghav's screenshots appeared small and awkwardly positioned instead of pinned front-and-center. Fixed the same way as detection: find the bframe's frame object via page.frames, get its owning iframe element via Playwright's frame_element(), and run the parent-chain-clipping-removal + pin styling as a callback on THAT element handle directly, so it operates in the correct document regardless of nesting depth. Also fixed the anchor-checkbox z-index step to scope to the bframe's own parent document (`bframe.ownerDocument`) instead of the top-level page. The success print is now conditional on the resize actually running, with an honest fallback message if the bframe element genuinely isn't reachable.

Syntax-checked (`py_compile`) and pre-flight (26/26) only — no live browser available here to trigger a real CAPTCHA and confirm both the alert and the resize now work end-to-end. This needs a live run that hits a CAPTCHA to fully confirm.


[1.2.9] 2026-07-08 — Raghav sent a live screenshot: a real, on-screen Google reCAPTCHA image challenge ("Select all images with cars") sitting on the review-module page, mid-run. Grepped "CAPTCHA DETECTED" (the alert print that fires before the email/notification) across all three of tonight's full debug logs — zero matches, in any of them, despite this screenshot proving a real CAPTCHA was actually showing. Confirmed root cause: `_captcha_actually_visible()` detected the bframe challenge iframe via `page.evaluate("() => document.querySelectorAll('iframe')...")`, which executes in `page.main_frame` only. Indeed's SmartApply form (the "review-m"/"question" iframe visible throughout every log's frame scans) is itself a nested iframe, and the reCAPTCHA widget — including its bframe challenge iframe — initializes inside THAT iframe's own document, not the top-level page's. A top-level `document.querySelectorAll('iframe')` can never see an iframe nested inside a different iframe's DOM, so this check silently returned `false` unconditionally, 100% of the time, regardless of whether a CAPTCHA was actually on screen.

This single bug explains nearly everything unresolved from tonight: (1) no CAPTCHA email/Mac-notification ever fired, so Raghav only found out by watching the screen directly; (2) every "Submit isn't landing" failure investigated in 1.2.6-1.2.8 (including a job where Raghav personally, manually clicked Submit with a real mouse and it still didn't go through) is fully consistent with an unsolved CAPTCHA overlay sitting on top of the real Submit button — no click, synthetic or human, can reach a button that's covered by an active challenge the pipeline never paused for or told anyone about; (3) very plausibly the recurring net::ERR_ABORTED session block that's shown up at nearly the same point in the query sequence in every run tonight — repeatedly re-clicking Submit against a page with an unacknowledged, unsolved CAPTCHA challenge is exactly the pattern Indeed's own anti-bot risk system would flag and clamp down on.

Fixed by rewriting `_captcha_actually_visible()` to use Playwright's `Frame.frame_element()` API instead of a DOM query: it now iterates `page.frames` (which was already correctly enumerating the nested bframe — confirmed by its own url appearing in every "Scanning N frame(s)" log line all along), finds the frame whose URL contains 'bframe', asks that frame object directly for its own owning `<iframe>` element (works regardless of nesting depth, since it doesn't depend on which document you search from), and checks that element's visibility + bounding box via Playwright's own `is_visible()`/`bounding_box()`. This is a more fundamental fix than the mouse-click/early-exit changes in 1.2.7 — those are still correct, worthwhile improvements for the case where a click genuinely doesn't land for other reasons, but this is very likely the actual root cause of why Submit kept silently failing across tonight's runs. Syntax-checked (`py_compile`) only — no live browser available here to trigger a real CAPTCHA and confirm the alert now fires; needs a live run (ideally one that hits a CAPTCHA) to confirm end-to-end.

Separately, clarified for Raghav (not a code change): a CAPTCHA timeout does NOT kill the whole Indeed run — `_check_and_handle_captcha` returning False just fails that one job and queues it in `_captcha_retry_queue` for a retry pass later (confirmed at indeed_apply_now.py:3822). What actually stops the full Indeed session and moves to LinkedIn is the separate, deliberate `INDEED_EMPTY_QUERY_BAIL_THRESHOLD` self-protection (from 1.1.3/1.0.7) triggering after 4 consecutive blocked searches — which, per the theory above, was very likely a downstream symptom of the CAPTCHA-detection bug itself, not a separate problem.


[1.2.8] 2026-07-08 — Raghav reported live: hit a CAPTCHA, solved it manually, clicked Submit himself, and it still wouldn't submit — then cancelled the run. Checked data/debug_logs/ for that run and the CAPTCHA/submit episode wasn't in the saved log at all, even though the 1.2.6 auto-logging feature was active. Root cause: `run_pipeline.sh` pipes Python's output through `tee`, and Python block-buffers stdout (instead of flushing line-by-line) whenever it isn't writing directly to a terminal — so the log file on disk lags behind real activity by a multi-KB chunk, and cancelling the process (Ctrl+C / force-quit) discards whatever hadn't been flushed yet, which is exactly the most recent and most useful part of the run. Fixed by exporting `PYTHONUNBUFFERED=1` and adding `-u` to both the indeed_apply_now.py and linkedin_apply_now.py invocations in run_pipeline.sh (run_indeed_now.sh already had `-u`, so it wasn't affected). This means any future cancelled run will have its output preserved right up to the moment it was killed.

Separately, read the actual log from that run (it did complete far enough to be useful) and found two things that update, not confirm, the 1.2.7 theory: (1) The job that got stuck (Woolpert — GeoBase Training Support Junior Analyst) never reached the Submit button at all — it looped on a required "How did you hear about us?" dropdown validation error for 4 steps before giving up, with zero submit-clicking involved. Checked `data/stuck_questions.json`'s saved page text for this job: that field is a required dropdown with a "Select an option" placeholder, but it's completely absent from the "Found N field(s)" list the fill logic scanned — the field-extraction JS (`smart_fill_step`, ~line 486) only queries `select, textarea` for native HTML select elements, and Indeed's SmartApply UI almost certainly renders this one as a custom ARIA combobox instead, which that selector can't see. This is a real, confirmed root cause for this specific stuck loop, not a guess — but actually fixing it means clicking to open a custom dropdown and clicking the right rendered option, which needs live DOM testing to get right rather than a blind patch; flagging for next investigation rather than shipping something unverified. (2) Despite this job never touching Submit, the run still hit the identical net::ERR_ABORTED wall on every Indeed search immediately afterward — same as the run 1.2.7 was built to address, and at nearly the same point in the query sequence both times. Since aggressive submit-clicking wasn't a factor this time, the 1.2.7 mouse-click/early-exit fix (still a correct, worthwhile improvement on its own) evidently isn't the fix for the block itself — this now looks more like a standing rate-limit/flag on the session or IP that trips after a small, consistent number of requests regardless of click behavior, not something reachable by a client-side code change. Flagging honestly rather than claiming this is solved: next step if it recurs is probably a fresh Indeed login and/or spacing out runs, not another click-mechanics patch. Syntax-checked (`bash -n`) only.


[1.2.7] 2026-07-08 — Read the auto-saved log (data/debug_logs/run_20260708_185204.log) from tonight's run instead of asking Raghav to paste it — the 1.2.6 auto-logging worked as intended. Confirmed the 1.2.6 preview/review button fix is working: every submit attempt this run correctly targeted "submit your application," never "preview." But that exposed the real, deeper bug: checked the new data/stuck_submits.json diagnostic capture (also new in 1.2.6) and found the page text saved after all 8 submit attempts on the Computer Enterprises Inc job was STILL the review page ("100%... Review your application... Submit your application") — the click never actually advanced the page, on any of the 8 tries. Playwright's own real (trusted) click failed silently on every attempt too (fell straight to the JS fallback each time) — the tell, since the page explicitly says "protected by reCAPTCHA," is that a raw JS element.click() dispatches an untrusted DOM event, which some of Indeed's gated buttons appear to silently ignore: the DOM shows a click happened, but the real submit handler behind it never fires. Directly after those 8 rapid-fire clicks (spanning ~90s), every single subsequent Indeed search failed with net::ERR_ABORTED on Page.goto for the rest of the run (LinkedIn, a completely separate browser/session, ran fine in the same run — rules out a general network problem). Correlation isn't proof, but it's a strong, well-timed signal that hammering an unresponsive Submit button with 8 synthetic clicks in rapid succession is what got the session flagged, which matches the alert email's own suspicion ("Indeed may be flagging this browser/IP").

Three changes, all scoped to the actual click/retry mechanics, nothing else touched: (1) `_click_nav()` and `_click_submit_btn()` now try a real Playwright `page.mouse.click()` at the button's bounding-box center — a trusted, browser-level input event — before falling back to the raw JS `.click()`, since a trusted click is far more likely to satisfy whatever gate (reCAPTCHA or otherwise) is rejecting the synthetic ones. (2) `_click_nav()` now logs the actual Playwright exception when its first click attempt fails instead of a bare `except: pass`, so the next time this happens there's a real reason on record instead of a guess. (3) The submit-retry loop now tracks whether the page (URL + first 2000 chars of body text) is byte-identical across attempts; if it doesn't change for 2 consecutive attempts (3 total), it stops immediately instead of grinding through all 8 — same diagnostics still get saved to data/stuck_submits.json either way. This directly reduces how many synthetic clicks get fired at an unresponsive button in one job, which is the most likely lever available to reduce the chance of tripping Indeed's bot detection again. Not fixed here: whether the mouse-click actually gets past the reCAPTCHA gate is unconfirmed — needs a live run to know if Submit actually goes through now, or if this is a structural block that needs a different approach (e.g., a real human click via the visible browser window during a CAPTCHA-style pause). Syntax-checked (`py_compile`) only — no live browser available in this environment to test against a real Indeed session.


All changes to the pipeline are logged here.
Format: `[VERSION] YYYY-MM-DD — What changed and why`

[1.2.6] 2026-07-08 — Analyzed the 17:00-17:35 Indeed run (6 jobs, 0 applied, all "form walk ended") using the actual saved run log instead of asking Raghav to re-paste it, and found three separate bugs hiding behind that one generic reason string:

(1) Real, confirmed root cause for HackerEarth: `_click_nav()`'s JS fallback (used when Playwright's own button click fails) matched buttons by bare keyword `'review'` — but `'preview what the employer sees'` contains `'review'` as a substring (`preview` = `p` + `review`), so on forms where a Preview button renders before the Submit button in DOM order, the fallback clicked Preview instead of Submit, over and over, burning all 8 submit-retry attempts on the wrong button while `_get_nav_buttons()` had correctly identified `'submit your application'` the whole time. Fixed by matching the full phrase `'review your application'` (matches what `_get_nav_buttons()` already uses) instead of bare `'review'`, and added `'preview'` to the exclusion list as a second line of defense.

(2) Sun River Health, Proteam Solutions, shark analytics, and Atlantic IT Solutions all show the submit button disappearing from the page (a strong success signal — Indeed replaces it with a confirmation page) followed by repeated "No submit button found" / "No nav buttons found", then a false "Failed: form walk ended" after all 8 attempts. Root cause: `_is_confirmed()`'s `CONFIRM_PHRASES` list is a fixed set of exact phrases, and the give-up path at the end of the submit-retry loop captured zero diagnostics — so there was no way to tell "genuinely stuck" apart from "actually succeeded, confirmation page just used different wording" without guessing. Two changes, both conservative: added several more common confirmation phrases to `CONFIRM_PHRASES` (pure text-matching additions, can only catch more real successes, can't cause new false positives on failure pages). Separately, and more importantly: the give-up branch now saves the page's URL, company/title, and up to 4000 chars of page text to a new `data/stuck_submits.json` (same pattern as the existing `data/stuck_questions.json`), so the next time this happens there's real evidence to look at instead of console text alone. Did NOT add a "no submit button = assume success" heuristic — that's a guess that could cause a real failure to get silently marked as applied, which is worse than an honest "unconfirmed." This needs a live run to know whether the broadened phrase list actually catches these four; `data/stuck_submits.json` is the fallback if it doesn't.

(3) Winsupply's stuck loop (open gap flagged in 1.2.5) was NOT the same widget as the EEO page 1.2.5 targeted — checked `data/stuck_questions.json` for the actual entry: a single-checkbox "Agree" field (Indeed's own group name is literally `SINGLE`, one option: `Agree`). The cache already has the right answer (`claude_answers.py`: `"single": "agree"`), so this isn't a caching/answer problem — it's that the generic radio/checkbox-group fill JS matches by comparing the answer text against each option's `value` attribute and `<label for>` text, and this specific widget has neither a matching value nor a proper `<label for>` element, so the text-match loop never finds anything to click and the box never gets checked, silently blocking Continue. Added a narrow, guarded fallback in the same JS: only when a checkbox group has exactly one option AND the cached answer is clearly affirmative (`agree`/`yes`/`i agree`/`i understand`/`accept`/`confirm`), check that sole option directly via the same click → label-click → wrapper-click escalation already proven in 1.2.5, rather than requiring a text match this widget variant can't provide. Scoped tightly (single-option groups only, affirmative answers only) so it can't misfire on genuine multi-option questions. Not yet live-verified against the actual Winsupply page.

Also implemented Raghav's explicit request: `run_pipeline.sh` and `run_indeed_now.sh` now `tee` their full console output (stdout+stderr) to a timestamped file under `data/debug_logs/run_<timestamp>.log` on every run, automatically, keeping the last 10 runs (older ones auto-deleted). This means future failures can be diagnosed by reading that file directly instead of Raghav manually copy-pasting terminal output. Syntax-checked both scripts (`bash -n`) and verified the `exec > >(tee -a "$LOG_FILE") 2>&1` redirection actually writes both to the file and the live terminal in an isolated test before shipping.

All indeed_apply_now.py changes syntax-checked (`py_compile`); none of them have been live-verified against a real run yet — that's the natural next step. None of these four changes touch each other's code paths, so they can be evaluated independently from the next run's log.

[1.2.5] 2026-07-08 — Indeed is Raghav's primary job-lead source, so the EEO/demographic-page stuck loop (open gap noted in 1.2.4) is worth a real attempt rather than just logging it for later. Best available lead: the resume-selection special case already proves, in this exact codebase, that some of Indeed's custom widgets only wire their real click handler to the visible `<label>`/card wrapper, not the underlying `<input>` — that's literally why it has a "click the parent card" step with the comment "Indeed needs the whole card clicked." The generic radio/checkbox group fill routine (used for the EEO page and everything else) never did this — it only ever clicked the raw input. Applied the same pattern there: after the direct `opt.click()`, if the input still isn't checked, also click its `<label for=...>` and the nearest `label/li/[role=radio]/[role=option]/[class*=card]` wrapper. Guarded with `!opt.checked` checks before each escalation step specifically because checkboxes (unlike radios) toggle on every click — an earlier draft of this fix would have silently unchecked already-successful checkbox answers by clicking the label a second time regardless of whether the first click worked; caught and fixed before shipping. This is additive and gated behind "still not checked," so it can't regress the fields that already work correctly (degree/clearance page, confirmed working as of 1.2.3). Not yet live-verified against the actual EEO page — this is a well-reasoned, low-risk attempt based on a proven pattern elsewhere in this file, not a confirmed fix like 1.2.3/1.2.4. Needs a live run to know if it actually clears the demographic-questions-module stuck loop.

[1.2.4] 2026-07-08 — Live-verified 1.2.3 fixed exactly what it targeted: same job (Interactive Process Technology LLC) re-run, the degree/clearance page that used to loop forever now shows `✗ QA FILE answer 'Yes' doesn't match any option ... treating as miss` and correctly gets Claude to answer it (Master's Degree, None, etc.) — 20/29 fields filled vs 15/29 before, no repeat loop on that step. But the job still failed, further downstream, on a genuinely different bug: the demographic/EEO self-identification page (`demographic-questions-module`). Checked `data/stuck_questions.json` for ground truth instead of guessing — found the resume-selection special case (used when Indeed shows resume-filename radio buttons) has a real bug at its return statement: `return clicked or 1` reports success whether or not a radio was actually found and clicked. Confirmed via the SAME stuck-questions log (Winsupply/Business Intelligence Analyst, same run): Indeed's own upload silently failed on the resume-selection page ("We couldn't upload your resume file. Wait a moment and then try again.") — 0 radios existed to click, but the function reported success anyway, so Continue kept getting clicked against an empty widget for 4 steps before giving up. Fixed: when 0 radios are found AND a file input is present AND the page shows an upload-failure message, retry the actual upload (`set_input_files` against `config.RESUMES_DIR / resume_filename`, 3 attempts with escalating delay — same pattern as the existing `_upload_resume()` retry) before re-scanning for the radio. This is additive-only — the only new code path fires in a case that was already 100% broken, so there's no regression risk to the working path. Separately: the demographic/EEO page's own root cause is NOT fixed here — `data/stuck_questions.json`'s saved page text was truncated at 1000 chars, cutting off exactly before the actual required-field markers, so there wasn't enough evidence to patch it without guessing blind. Bumped the snippet capture to 4000 chars so the next occurrence is actually diagnosable. Known open gap, flagged for next investigation rather than guessed at.

[1.2.3] 2026-07-08 — Diagnosed the "form won't fill" reports from the first two Indeed live runs post-profile-reset (both jobs failed with "form walk ended"): NOT a cache-vs-Claude routing problem — Claude was called and answered correctly both times it was asked (3/3, 4/4). The real bug: several of Indeed's radio/checkbox questions have no discoverable legend text and fall back to an opaque internal ID as their label (e.g. `q_b74adbe8ed1ce66a90511cc535d5a422`). `qa_answers.py` has a blanket rule (line 1143) defaulting any such hash-labeled field to `"Yes"` — correct for simple Yes/No gate questions, which is most of what these IDs usually are, but Indeed reuses the same ID pattern for genuine multi-option questions too. On the Interactive Process Technology LLC job, one hash-labeled group was actually "highest level of education" (options: High School/Associate's/Bachelor's/Master's/Doctorate) and another was the clearance-level checkboxes — `"Yes"` matches none of those options, so the DOM fill's click-by-matching-option logic (line ~1023) silently never fires for that field, "Select an option" validation never clears, and the form loops on the same URL 4 times before giving up — all while every cache lookup keeps reporting a confident "hit," so Claude is never even asked, since a cache hit short-circuits before that fallback path. Added `_answer_valid_for_options()` in `smart_fill_step()`: any cached/QA-file answer for a field with a known `options` list is now checked against those options (same contains-either-way match the fill pass itself uses) before being trusted; a mismatch is treated as a cache miss and routed to Claude instead, which gets the field's real options list in its prompt and picks a valid one, which then gets saved back so future runs of the same field self-heal. Verified against all 8 hash-labeled fields from tonight's actual stuck run in an isolated unit test (not just py_compile) — correctly flags the 5 that were silently broken (degree, DoD certs, clearance, contact-method, "I understand") and correctly still trusts the 3 that were fine. Separately, job 1 (Bella Group) is NOT confirmed related to this bug — it filled every field fine, solved a CAPTCHA, clicked Submit, then got no further confirmation signal and gave up after 8 retries; genuinely unclear from the log whether it actually went through on Indeed's side or not — flagging for Raghav to check Indeed's Applied Jobs list directly rather than guessing. Not fixed here — needs a live watch to know if this is a separate bug or just a slow confirmation page.

[ops] 2026-07-08 — Even with a fully clean lock state (all three Singleton* files manually removed, confirmed empty, no "cleared stale" messages) Indeed's launch still failed identically: `TargetClosedError: BrowserType.launch_persistent_context: Target page, context or browser has been closed`, with the browser's crashpad warning showing the *exact same* value (`in_range_cast.h:38] value -634136515 out of range`) as the 12pm crash, four hours and two different PIDs apart — a deterministic failure, not random noise, and not a lock-file problem (1.2.2 ruled that out). LinkedIn's separate profile launched fine on the same machine/Chromium build throughout the same day, isolating the problem to the `.indeed_session` profile directory itself, not Chromium/Playwright/macOS in general. Given the profile's known history of scattered LevelDB `LOCK` files (Session Storage, IndexedDB, GCM Store, etc.) left behind by every unclean shutdown since the original dialog-crash bug (v1.0.4, 2026-06-28 — matches Raghav's "7-10 days ago it worked fine" estimate), concluded the profile itself is corrupted beyond what a code fix can address. Backed up (not deleted) `.indeed_session` → `.indeed_session_corrupted_backup_20260708_1615` per Raghav's approval; `launch_persistent_context()` will create a fresh profile at the same path on the next run. Requires a manual Indeed login in the new window on first launch. No code change — SESSION_DIR path unchanged, nothing to revert if this doesn't fix it (old profile is preserved in the backup folder, not gone).

[1.2.2] 2026-07-08 — Found the real reason Indeed kept failing to launch after 1.2.1: reproduced it directly — `SingletonLock`/`SingletonCookie` are symlinks whose *target* is a plain `hostname-pid`/numeric string, not an actual filesystem path, so `Path.exists()` (which follows symlinks and stats the target) always returns `False` on them, even when the symlink itself is sitting right there. The pre-launch stale-lock cleanup added in v1.1.5 only ever checked `.exists()`, so it has silently never cleared `SingletonLock` or `SingletonCookie` since the day it was written — only `SingletonSocket` (whose target happens to look like a real path) ever actually got removed. Confirmed live tonight: a manual run after a crash printed only "Cleared stale SingletonSocket" and left `SingletonLock` behind, and the next Chromium launch immediately died with `TargetClosedError: BrowserType.launch_persistent_context: Target page, context or browser has been closed` — Chromium's own instance check saw a Lock file but no matching Socket to verify it against, and bailed. Changed the check to `_lock_path.exists() or _lock_path.is_symlink()` so broken/stale symlinks are detected and removed regardless of what their target resolves to. This is likely the real cause behind a good fraction of the "launch fails right after a prior crash" pattern seen across the last couple weeks, not just today. Reproduced the exact bug and the fix in an isolated symlink test (not just py_compile) before shipping. Still recommend a live `python indeed_apply_now.py --limit 50` to confirm end-to-end.

[1.2.1] 2026-07-08 — Recurrence of the 1.0.4 dialog crash: today's 12pm run's Indeed browser died within seconds (`ProtocolError (Page.handleJavaScriptDialog): Not attached to an active page`) and Indeed produced 0 applications, then a subsequent relaunch attempt hit `Timeout 180000ms exceeded`. Root cause is deeper than 1.0.4 addressed: the Node.js err.log shows the crash comes from Playwright's own internal `DialogManager.dialogDidOpen → Dialog.close → Dialog.accept` auto-close path racing against page teardown — not from our `_safe_dismiss()` handler, so wrapping that handler in try/except (the 1.0.4 fix) can't catch it; it's an uncaught exception inside the Node driver itself. Two changes in `indeed_apply_now.py`: (1) `_safe_dismiss()` now also checks `d.page.is_closed()` before calling `dismiss()` — narrows one race window, though not the one that crashed today. (2) The real fix: `main()` had no cleanup path if the browser session died before reaching `browser.close()`, so the Chromium profile's SingletonLock/Cookie/Socket files were left behind, and the next launch attempt (same run or next scheduled run) hung on the stale lock until Playwright's own timeout — this is what produced the follow-on `Timeout 180000ms exceeded`. Registered an `atexit` handler right after `launch_persistent_context()` that unconditionally removes those three lock files on process exit, clean or crashed. Also added `INDEED_BROWSER_LAUNCH_TIMEOUT_MS` (config.py, default 60000) and passed it as `timeout=` to `launch_persistent_context()` so a genuinely stuck launch fails in ~1 minute instead of 3, if this ever recurs. Syntax-checked (`py_compile`); Playwright wasn't available to live-test in this session (no browser/login session outside Raghav's Mac) — run `python3 indeed_apply_now.py --dry-run` before the next live run to confirm the lock-release and faster-fail behavior. Also reviewed and confirmed 1.2.0's scoring toggle is working as intended: `USE_CLAUDE_SCORING = False` in config.py, both `indeed_apply_now.py` (line 3408) and `linkedin_apply_now.py` (line 1917) correctly branch to the free `jd_parser.ats_fit_score()` for fit-scoring — no Claude/Anthropic API call for the apply/skip decision. The small haiku API costs still showing in run summaries (e.g. today's LinkedIn run: 10 haiku calls, $0.0125) are from unrelated, cheaper Claude calls — LinkedIn's grey-zone legitimacy/trust check and vision-based form-field validation — not fit scoring, exactly as noted in 1.2.0.

[1.2.0] 2026-07-06 — Per Raghav's explicit request: default fit-scoring gate switched from Claude to the original pre-Claude free scoring method. Root cause of tonight's "everything scores 0%" was `anthropic` not being importable under `python3` (a python/python3 PATH mismatch on his Mac — `runjobs`/the scheduled launchd runs use plain `python` and were unaffected; this was specific to the manual `python3 run_all.py ...` commands used for testing tonight), but once found, Raghav decided he doesn't want the ongoing Claude API cost at all (~$1.68 tracked today across 3 scheduled runs) rather than fix the environment mismatch.

Found the pipeline's original scoring method still intact and unused in `jd_parser.py`: `compute_ats_score()` (5-dimension keyword/skills/experience/education/title composite, zero API cost) — this predates Claude integration (commit 28cd70e, "v5 — Claude AI scoring") and has stayed working the whole time as the engine behind resume-tailoring, just not wired into the apply/skip decision since Claude took over that role. Added `jd_parser.ats_fit_score()`, a thin wrapper returning the same dict shape as `claude_engine.score_fit()` so both `indeed_apply_now.py` and `linkedin_apply_now.py` can switch between the two with no other code changes. New config.py toggle `USE_CLAUDE_SCORING` (default `False`) and separate `ATS_FIT_THRESHOLD` (default 60, different scale from `FIT_THRESHOLD` — needs retuning after watching a real run). Flip `USE_CLAUDE_SCORING` back to `True` any time to restore Claude's judgment with zero other changes.

Known, demonstrated trade-off: tested `ats_fit_score()` against a deliberately mismatched JD (Registered Nurse posting) — it scored 61%, `apply: True`, above threshold, where Claude would reject it outright. In the live pipeline this specific case is still caught by the earlier off-domain-title filter (nurse jobs are blocked there already), but anything that passes the loose title-keyword filter and still has domain/seniority mismatches Claude would normally catch may now get approved. Not fixed here — flagging for Raghav to watch over the next few runs. Note: this only changes the fit-scoring gate — LinkedIn's grey-zone legitimacy check and Indeed's batched snippet pre-filter still make their own (smaller, cheaper) Claude calls independent of this toggle, so cost drops significantly but isn't fully zero; not touched in this change since it wasn't part of what was asked.

[1.1.5] 2026-07-06 — Fixed the exact crash from tonight's `runjobs` run: Indeed's startup cleanup deleted Chromium's SingletonLock/SingletonCookie/SingletonSocket files whenever they existed, with no check that the owning process was actually dead. An earlier dry-run's Chrome was still alive when a fresh run started, cleared its "stale" lock, and launched a second Chrome on the same `.indeed_session` profile — Chrome's own instance check rejected the new one, surfacing as `TargetClosedError: BrowserType.launch_persistent_context: Target page, context or browser has been closed` before a single search ran (0 Indeed applications, not related to the 1.1.3/1.1.4 fixes, which never got a chance to execute). Added `_lock_owner_pid()`/`_pid_alive()` to read the PID out of each lock file and check via `os.kill(pid, 0)` before deleting; if the owner is still alive, the run now skips itself cleanly with a "session already in use" email instead of guaranteeing a crash. Syntax-checked (`py_compile`); next run with a genuinely stale lock (process actually dead) should still clear it exactly as before, and a run with a live colliding process should now exit cleanly with the new alert instead of throwing a traceback — both cases need a live check to confirm.

[1.1.4] 2026-07-06 — Live-watched a dry-run tonight after the manual Indeed re-login (confirmed the 1.1.1/1.1.3 fixes worked: "Data Engineer" query found 103 cards, "Junior Data Engineer" 60 — back to healthy yield). Noticed a handful of cards coming through with a blank company name (e.g. "— Quality Data Analyst - Automotive"), and every one of those landed at a flat 0% fit score. These are almost certainly Indeed's own "recommended near you" filler cards padded into the results list when a narrow query (entry-level + last-7-days + exact title) runs out of genuine matches — they carry a real data-jk so the scraper can't tell them apart from real hits at the DOM level, but they reliably fail JD extraction downstream. Added a check right where title/domain/staffing are already screened for free: if `company` comes back blank, skip immediately instead of paying for a job-page load and a Claude scoring call to learn it's a 0% anyway. No change to any real filtering behavior — genuine listings all have a company name. Syntax-checked (`py_compile`); next dry-run should confirm the "Blank company name: ... skipping" line only ever fires on the same filler cards, never on a real listing.

[1.1.3] 2026-07-06 — Fixed a gap in Indeed's block-detection: `INDEED_EMPTY_QUERY_BAIL_THRESHOLD` (1.0.7) only trips on *consecutive* zero-card searches, but a soft-blocked/Cloudflare-throttled session doesn't always return exactly 0 — sometimes 1-2 cards trickle through, which resets that counter and lets the run grind for hours at a tiny fraction of normal yield. Confirmed live tonight: the 6pm run found 45 cards across 54 searches in 76 minutes (avg <1 card/search) and never tripped the existing bail. Added a second, rolling-average check — `INDEED_LOW_YIELD_MIN_QUERIES`/`INDEED_LOW_YIELD_AVG_CARDS` in config.py (default: after 6 queries, bail if average cards/query is below 5, vs. a healthy ~45/query by design) — reusing the same alert-and-stop path as the existing zero-streak bail. Applied-per-day had collapsed from 10-20/day (late June) to 0-2/day (first week of July), consistent with a soft-blocked session going undetected for the full run length. Syntax-checked (`ast.parse`/`py_compile`); a live/dry-run watch of the next Indeed run is still needed to confirm the bail fires correctly, since it couldn't be safely tested against the same browser profile as a run already in progress. Root cause of the block itself is still believed to be a stale/flagged Indeed session per 1.1.1 — this only makes the pipeline detect and stop faster, it doesn't fix the underlying block; a fresh manual login is still required.

[1.1.2] 2026-07-06 — Same fix as 1.1.1, applied to Indeed: `build_indeed_url()` in `indeed_apply_now.py` had no experience-level filter either (only `q`, `l`, `sort`, `fromage`). Live-watched a run this morning — every scored job (Fire Data Analyst, MES Reporting Analyst, IT Business Analyst, etc.) landed at 28-58% fit, all below the 60% threshold, because the queries were pulling adjacent Business/Reporting/IT-Analyst roles, not real Data Engineer matches. Added Indeed's native `explvl=entry_level` param to the search URL — one line, no other logic touched. Syntax-checked (`py_compile`) and URL-encoding verified; watch the next Indeed run's fit-score distribution to confirm.

[1.1.1] 2026-07-06 — Diagnosed why applications dropped from ~20-30/day (mid-June) to near 0: (1) Indeed's session was logged out and every search got Cloudflare-blocked — needs a fresh manual login, not a code fix. (2) LinkedIn's `build_url()` in `linkedin_apply_now.py` had no LinkedIn-native experience-level filter (`f_E`) — queries like "Data Engineer Entry Level" are fuzzy keyword matches, so LinkedIn kept returning Senior/Lead postings anyway (61/137 cards skipped as senior/lead in one run, 65/137 off-domain, only 8 ever reached scoring). Added `f_E=2,3` (Entry level + Associate) to the search URL so filtering happens at LinkedIn's end instead of relying on keyword luck. Syntax-checked (`py_compile`) and URL-encoding verified; a live browser dry-run (`python3 linkedin_apply_now.py --dry-run`) should be run on the next LinkedIn pass to confirm card counts/quality improve before a live run.

---

[1.0.1] 2026-06-28 — Added citizenship/visa ineligibility keywords to CLEARANCE_KEYWORDS in config.py. Jobs requiring US citizenship, green card, permanent residency, or blocking OPT/CPT/visa holders are now skipped immediately, same as clearance jobs.

[1.0.2] 2026-06-28 — Blocked 5 repeat-failing staffing firms (russell tobin, glocomms, come near, allied resources, bayone solutions) in FAKE_JOB_COMPANY_WORDS. Fixed FIT_THRESHOLD split: claude_engine.py now reads from config.py (60%) instead of a hardcoded default of 65%.

[1.0.3] 2026-06-28 — Cross-platform dedup: Indeed now merges the LinkedIn apply_log at startup so jobs already applied via LinkedIn are skipped by Indeed's existing already_applied() check.

[1.0.4] 2026-06-28 — Fixed ProtocolError/TargetClosedError crash in both linkedin_apply_now.py and indeed_apply_now.py. Dialog handler lambda replaced with _safe_dismiss() that swallows exceptions when a JS dialog fires after the browser/page is already closed. This was killing entire evening runs in under 30 seconds.

[1.0.5] 2026-06-28 — Trimmed LINKEDIN_QUERIES from 53 to 20. Removed narrow tool-specific and overlapping queries (PySpark/Azure/AWS/GCP/Databricks/Snowflake/dbt variants, niche roles). Broad entry-level queries kept. Cuts navigation overhead roughly in half (~150 min vs ~320 min) with no ToS change and minimal job coverage loss.

[1.0.6] 2026-07-02 — Fixed LinkedIn crash that the 1.0.4 dialog-handler fix didn't actually catch: `job_ids = page.evaluate(...)` (search results scrape) and `extract_right_panel(page)` (per-job detail scrape, called once per card) were both unguarded. When the browser/page died mid-run (e.g. a JS dialog firing right after navigation), these raised straight out of the whole run — matches the "LinkedIn: error: Page.evaluate: Target page, context or browser has been closed" failure seen in the 2026-07-02 afternoon run log. Both call sites now catch PWError, reopen the page, and either retry or skip the current job/query instead of ending the entire LinkedIn session.

[1.0.7] 2026-07-02 — Indeed no longer grinds for hours when the session is blocked. Same afternoon run showed a 596m and a 607m stall: CAPTCHA_COOLDOWN logic reset its counter after every 5-min cooldown with no cap, so an unattended (scheduled) run — nobody available to solve CAPTCHAs — could cooldown/retry/fail in a loop all day. Added `CAPTCHA_MAX_COOLDOWNS_PER_RUN` (config.py, default 2): after this many cooldowns with no successful solve, the run stops itself and sends one alert instead of looping. Also added `INDEED_EMPTY_QUERY_BAIL_THRESHOLD` (default 4): the same run showed every search returning "Found 0 cards" back-to-back (net::ERR_ABORTED on every page load) — now Indeed bails after N consecutive empty-result searches instead of burning the rest of the query list against a wall.

[1.0.8] 2026-07-02 — Fixed a real cause of stuck/failed applications: numeric fields ("How many years of X experience?") rejected by the site's own validation with "Answer must be a valid number (no decimals)" whenever Claude answered with a fraction (e.g. "2.5"). Indeed and LinkedIn both now round any number-field answer to the nearest whole number before filling, and the Claude prompts in both scripts now explicitly say whole numbers only. Also fixed data/stuck_questions.json always logging `"fields": []` for every stuck application (182/182 entries checked had this) — the field it read, `form_fields_last_seen`, was either never assigned (Indeed's fallback check `'form_fields_last_seen' in dir()`) or assigned via a broken `isinstance(form_ctx, dict)` check that's always False since form_ctx is a Playwright Frame, not a dict. Replaced with a proper module-level `_last_seen_fields` set inside smart_fill_step() itself, so future stuck entries will actually show which question(s) blocked the form. Known gap not fixed here: Indeed percentage-slider questions (0%/25%/50%/75%/100% style) aren't handled by the fill logic at all — seen in the Flowserve stuck entry (2026-06-30) — flagging for a follow-up since it needs live DOM inspection to fix safely.

[1.0.9] 2026-07-02 — New feature per Raghav's request: skip staffing agencies and consulting firms entirely, on both Indeed and LinkedIn, even legitimate ones (this is a preference, not fraud detection — separate from the existing FAKE_JOB_COMPANY_WORDS/COMPANY_WHITELIST fraud system, and takes priority over the whitelist). New shared module `staffing_filter.py` with `is_staffing_or_consultancy(company, description)`, driven by two new config.py lists: `STAFFING_CONSULTANCY_COMPANY_WORDS` (named staffing agencies + generic keywords like "staffing"/"consulting"/"consultancy" + IT-services/Big-4 firms like TCS, Infosys, Deloitte, Accenture, McKinsey) and `STAFFING_CONSULTANCY_DESC_SIGNALS` (description language like "on behalf of our client", "our client is seeking"). Toggle: `SKIP_STAFFING_CONSULTANCY` (default True). Indeed checks it twice — once cheaply on the company name before loading the job page, again on full JD text after — LinkedIn checks it once with the description it already has. Verified against test cases (Robert Half/TCS/Deloitte/consulting-named companies correctly excluded; Google/Databricks/generic "Solutions"-named companies correctly pass; no false positives on "Turkey Hill", "Money.com", "Keystone Bank").

[1.0.10] 2026-07-02 — Fixed the log-schema bug found during pro-level analysis: Indeed's cross-platform dedup (from 1.0.3) merged LinkedIn's entire apply_log.json directly into the in-memory `log` list that ALSO gets saved back to indeed_applied_log.json after every job. Two problems: (1) LinkedIn's log schema uses a "note" field for the skip/fail reason, Indeed uses "reason" — so every LinkedIn entry that leaked into Indeed's log looked reason-less. (2) Every Indeed run re-saved LinkedIn's entire history back into indeed_applied_log.json, which had ballooned to 119,126 records (49MB) — only 3,438 (2.9%) were actually Indeed's own. Fixed by keeping the merged LinkedIn data in a separate `dedup_log` used only for the already_applied() lookup; `log` (what gets appended-to and saved) now only ever contains Indeed's own entries. Also did a one-time cleanup of the existing indeed_applied_log.json: backed up to indeed_applied_log.json.bak_pre_cleanup_20260702_181631, then filtered out all linkedin.com-URL entries — file dropped from 119,126 records / 49MB to 4,953 records / 3.7MB. Failed-entries-with-empty-reason dropped from 97% to 12.1% (the residual 12% are genuine edge cases like bare exceptions with no message, not a schema issue).

[1.0.11] 2026-07-03 — Fixed three related "pipeline goes silent" bugs reported live during a CAPTCHA (Raghav solved it and submitted, pipeline kept waiting the full 10 minutes anyway):
(1) `_check_and_handle_captcha()`'s `still_captcha` check only tested whether the reCAPTCHA `bframe` iframe was still *attached* to the page (`"bframe" in f.url`). Google's reCAPTCHA never removes that iframe after a solve — it just hides it — so the check could never go False unless the whole page happened to hard-navigate. Replaced with a visibility check (computed `display`/`visibility`/bounding-rect) inside `page.evaluate`, with the old presence check kept as a fallback if evaluate throws.
(2) The `already_confirmed` fallback (looks for "application submitted" etc.) only read `page.body.innerText` on the top-level document, but Indeed's apply UI — including the post-submit confirmation — renders inside a nested iframe (the auto-click-Submit code a few lines down already knew this and loops `page.frames()`; the text check didn't). Now scans every frame's `document.body.innerText`, not just the top-level page.
(3) Separately: Indeed's "stop early after N consecutive 0-card searches" bail (added in 1.0.7 for the Cloudflare-block case, `INDEED_EMPTY_QUERY_BAIL_THRESHOLD`) never sent an email, unlike its sibling CAPTCHA-cooldown-exceeded stop which does. So a Cloudflare-blocked run went completely silent — no real-time alert, and since it also applied 0 jobs, `notify_session_done()`'s `if applied == 0: return False` guard suppressed the end-of-run summary too. Added a `notifier.send_alert()` call at the bail point so a blocked/Cloudflare-stopped run now emails immediately, matching the CAPTCHA-cooldown behavior. (Note: `notify_session_done`'s 0-applied suppression itself was left as-is — not touched in this change.)

[1.0.12] 2026-07-03 — Fixed the back-to-back CAPTCHA email spam reported right after 1.0.11 shipped. 1.0.11 made the wait-loop's "is it still showing" check visibility-aware, but missed that `_check_and_handle_captcha()`'s entry gate (`captcha_visible`, checked at the very top of the function, and called again after every Submit-retry attempt) still used the old presence-only check. Since reCAPTCHA never removes the bframe iframe from the DOM — only hides it — that gate stayed True for the rest of the job's session once a CAPTCHA had appeared even once. Result: every one of the up-to-8 Submit-retry attempts in the same job re-triggered the full "CAPTCHA DETECTED" alert + email + Mac notification, then immediately self-resolved via the (correctly fixed) wait loop — looking like a solved CAPTCHA kept reappearing every ~8 seconds. Fixed by extracting one shared `_captcha_actually_visible()` helper (the visibility check from 1.0.11) and using it at both the entry gate and inside the wait loop, so there's only one place this logic lives and it can't drift out of sync again.

[1.1.0] 2026-07-03 — New feature per Raghav's request: batched snippet pre-filter for Indeed search-result pages, instead of opening every surviving card's job page one at a time to find out if it's worth applying to. Indeed's card scrape (`_page_cards` in indeed_apply_now.py) now also captures each card's short on-page snippet (with an innerText fallback if Indeed's snippet class names change). New `claude_engine.prefilter_cards_batch()`: takes every card that already passed the existing free filters (title level, domain, staffing/consultancy, already-applied dedup) for one query, and makes a SINGLE haiku call covering the whole batch (up to ~45 cards) asking only "obviously wrong or worth a closer look?" using just title+company+snippet. Cards it flags obviously-wrong (senior/lead titles, unrelated domain, staffing agency) are skipped with zero page loads; everything else still goes through the existing full-JD score_fit() check exactly as before — this only adds a coarse gate in front of it, it doesn't replace the real scoring. Deliberately fails open (treats every card as "worth opening") if Claude is unavailable or the response can't be parsed, verified with unit tests simulating both a normal batched response and a garbage/unparseable one — a bad API response can never silently drop a job that would've been a real fit. Side benefit: fewer job-page navigations per query means fewer chances to trip Indeed's Cloudflare check, which has been the main cause of blocked sessions this week.

## [1.0.0] 2026-06-28
- Added `PIPELINE_VERSION` to `config.py` — version now prints in every log
- Added `INDEED_ENABLED`, `LINKEDIN_ENABLED`, `WORKDAY_ENABLED` flags to `config.py` — can now turn off a single platform without touching its code
- Created `CHANGELOG.md` — this file

---

## How to log a change

Before making any edit:
```bash
bash safe_update.sh start "describe what you're trying"
```

After the change works:
1. Bump `PIPELINE_VERSION` in `config.py` (e.g. 1.0.0 → 1.0.1 for a fix, → 1.1.0 for a new feature)
2. Add a line here: `## [1.0.1] YYYY-MM-DD — what changed`
3. Run `bash safe_update.sh keep` to merge and save to GitHub

If the change breaks something:
```bash
bash safe_update.sh discard   # back to last working version instantly
```

---

## Version guide
- `1.0.X` — small fixes and tuning (fit threshold, delays, blocked companies)
- `1.X.0` — new feature or filter (new platform, new fraud detection layer)
- `X.0.0` — major rewrite or structural change
