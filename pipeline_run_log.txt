============================================================
  Job Pipeline — Evening Run Log
  Date: 2026-06-08  (evening)
============================================================

STATUS: ❌ COULD NOT EXECUTE — Sandbox network restriction

STEP 1 — Seed cache:        ✅ SUCCESS
  - 313 answers cached (14 empty/skipped)
  - 294 questions in cache

STEP 2 — Indeed (50 apps):  ❌ BLOCKED
STEP 3 — LinkedIn (50 apps): ❌ BLOCKED

SUMMARY:
  Indeed applications submitted:   0
  LinkedIn applications submitted: 0
  Total:                           0

ROOT CAUSE:
  The scheduled task runs inside the Cowork sandbox. The sandbox
  network proxy blocks outbound connections to indeed.com and
  linkedin.com (HTTP 403 Forbidden). Playwright's Chromium browser
  cannot reach those domains from this environment.

  Additionally, smtp.gmail.com cannot be resolved from the sandbox,
  so the summary email could not be sent.

ACTION REQUIRED:
  Run the pipeline manually on your Mac:

    cd ~/job_pipeline
    python seed_cache.py           (already done — cache is fresh)
    python -u indeed_apply_now.py --limit 50
    python -u linkedin_apply_now.py --limit 50

  Or re-schedule this task to run as a local Mac launch agent
  (launchd) instead of via Cowork, so it executes natively with
  full internet access and your saved browser sessions.

============================================================
